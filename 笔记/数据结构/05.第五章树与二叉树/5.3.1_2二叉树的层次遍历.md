各位同学大家好，在这一小节中我们会学习二叉树的程序遍历，也就是一层一层的遍历，这个二叉树的各个节点。那像这棵二叉树，如果我们用程序遍历的话，那最终得到的遍历序列应该是abcdefghijkl。是这样的一个序列好，那要实现二叉树的程序遍历，我们需要设置一个辅助的队列，首先第一步要做的是我们要把根结点放到队列当中。接下来需要循环重复这样的一个动作，每次我们要判断这个队列是否为空，如果队列不空的话，那么我们就让队头元素出队。同时，
要访问这个结点，所以第一个被访问到的结点就是a这个结点，也就是根结点，另外我们还需要把这个结点的左右孩子都插入对位。先插入左孩子，再插入右孩子，那a这个结点的左孩子是b右孩子是c，所以接下来应该是b和c两个结点依次入队。好再往后就是不断的重复刚才这个步骤，也就是先判断这个队列是否为空，那由于此时队列非空，所以我们让对头结点出队。那接下来要访问的就是b这个结点，同时要把b结点的左右孩子依次放到对位那b结点的左孩子是d。右孩子是e。
好，接下来这个队列依然是非空的，所以我们让对头元素c出对访问c这个结点，然后让c的左右孩子，也就是f和g。分别入队，那再往后的动作都是类似的，每访问一个节点之后，都需要把这个节点的左右孩子依次放到对位。然后再让下一个节点出队并进行访问，那像e这个节点，它是没有左右孩子的，所以访问完e之后是不会有任何一个节点入队的。所以接下来我们应该让f这个节点出队，然后让它的左右孩子依次入队。
好以此类推。最下面一层的这些节点，他们都没有左右孩子，所以在访问这几个节点的时候，不会有任何一个新的节点入队好，那用这种方法我们就完成了对二叉树的程序遍历。好，那接下来我们把刚才分析的逻辑用代码把它实现一遍，首先我们需要初始化一个辅助队列，那这儿我们使用的是一个链队列。而不是一个顺序队列，因为我们难以估计我们要访问的这棵树，它到底有多少个结点，所以使用了链队列，这样的话可以方便队列节点的拓展。
好，那我们这儿只给出链对列的数据结构定义。具体怎么初始化，怎么让一个元素入队，这些是我们在第三章当中学过的东西，这就不再重复。那需要注意的一点是，刚才我们一直在说某一个结点让它入队，但是其实我们并不需要在队列当中保存一整个结点的真实数据。其实我们只需要保存这个节点的一个指针就可以了。因为保存这个节点的指针肯定要比保存这个节点本身需要的空间会少很多。所以你看我们让一个结点入队的时候，其实真正入队保存的是这个结点的指针，而不是这个结点本身。理解了算法思想之后，
其实这个代码是很简单的，那这儿我们用了一个while循环当队列不空的时候就会一直循环的，让对头元素出对。然后访问此次出对的这个结点。那你可以自己定义访问一个节点的时候，需要做一些什么操作，比如说打印这个节点的值等等。好，接下来如果当前访问的结点p，它有左孩子的话，那么就让它左孩子入队，如果有右孩子的话，也需要让右孩子入队。然后一直循环，直到队列为空好的，
那这就是二叉树的层次遍历，需要用一个队列来辅助实现。好的，那这一小节我们学习了，树的层次遍历算法，关键在于我们需要设置一个辅助队列，算法本身并不复杂，那大家可以借助这个小节的内容，自己暂停回忆一下，和队列相关的那些知识点。好的，那以上就是这一小节的全部内容。