## 串的顺序存储

好了，那这个小节我们会探讨串的存储结构，那上一小节我们提到过<u>串，其实是一种特殊的线性表</u>，所以<u>我们可以用什么样的存储结构来实现线性表？</u>

<u>同样的就可以用什么样的存储结构来实现串，只不过线性表当中存储的是elemType是某一种数据元素。但是串里边我们把elemtype改成char不就行了</u>，对吧？用它来存储字符。好，那首先来看一下，用顺序存储的方式实现串和线性表一样，我们可以像这样定义一个静态数组来存储这个字符串。这是静态数组的最大容量，
然后这个length表示的是当前这个字符串，它实际有多长？那采用这种方式，当你声明一个静态的字符串的时候。系统会在内存当中给你开辟这样的一片连续的空间，用来存放你的各个字符，而每个叉每个字符是占一个字节。当然了，还需要一小片空间来存储你的length，这个变量，那之前我们学过像这种<u>静态数组的缺点呢，是它的长度不可变。如果你二五五这个长度存不下的时候，想要拓展它，那就是不可能的，</u>
所以我们的<u>课本当中把它称为定长的顺序存储</u>。其实它就是一个静态数组和线性表一样的好，<u>那如果想让它可变长的话，我们就可以用动态数组的方式来实现，也就是在你的结构体里边定义一个。一个这样的基地址的指针，然后用my lock来申请一整片的连续的存储空间，让你的基地址指针指向这一片空间的起始地址</u>。

<u>那之前我们说过my lock函数申请的这一片内存空间是属于内存里边的堆区</u>。

<u>所以我们的课本上把这种实现方式称为堆分配存储，其实它就是一个动态数组，那堆区的这一片内存空间，你需要手动的free，也就是调用free函数。把这一片空间给释放回收</u>，

而如果<u>用上面这种方式实现的话，那给你分配的这一片存储空间是会由系统自动的回收的。当你的函数执行结束之后，这一片空间就会被自动回收</u>。好的，那这些东西其实和线性表都是一样的，只不过是我们把这儿存储的数据元素换成了叉。也就是字符那类似的，

<u>如果你在简答题当中碰到让你回答，用这样的顺序存储的方式来实现的串有什么优缺点的话，其实你可以结合我们在顺序表当中学习的那些知识点。进行思考，比如说最大的一个优点，当然就是随机存取</u>，对吧？
你可以立即找到任何一个你想找的那个字符。<u>但是缺点呢，就是插入和删除，也就是这片空间的拓展和收缩，不太方便，那这些都和顺序表是一样的</u>。

![image-20230920142024143](/Users/yuebinghui/Documents/program/github/note/images/image-20230920142024143.png)

不同的教材里边，顺序存储的实现细节会有一些区别。比如，像我们刚才提到的这种方案，就是申请这样的一个数组。然后还会有一个专门的变量用来记录这个字符串，它到底有多长？它的教材会用啊char[0]也就是这个数组的第一个位置来充当lens，这个变量的角色。
就可以在这个地方记录你的这个字符串，它到底有多长？那这么做的优点是各个字符的位序和数组下标都是一一对应的。因为我们说过，位序是从一开始对吧？一二三四这样。所以如果我们从数组下标为零的位置开始的话，就需要把位序再减一，这样会稍微更麻烦一些，当然这无关紧要，你想要哪种实现方式都可以。好，除了用一个专门的变量来记录字符串的长度之外，还有的地方实现字符串是这样实现的，就是它不会明确的明显的记录它。
<u>方案三，它的长度到底是多少，而是会在字符串的末尾处插入一个特殊的字符，叫做\0。这个字符对应的ask编码是零，也就是八个二进制的零，那想一下采用这种方案有什么缺点？很明显，如果我们想要知道这个字符串，它到底有多长的话，我们得一个一个遍历吧，就是从头到尾扫描看看什么时候扫描到这个\0</u>。然后我们再来统计它到底有多长，对吧？<u>所以如果经常需要访问到这个数组的长度的话，那么这种方案就是不太可取的</u>。

诶，<u>再回过头来看这个第二个方案</u>，这种方案我们虽然说节省了末尾的这样一小 片空间。但是由于我们的这些数组是char型的，对吧？<u>每一个char型的字符其实只占一个字节，也就是八个比特位。那么就意味着，如果我们用char[0]充当lens的角色，那char[0]其实它本身也只有一个字节的大小。它所能表示的数字范围也就是零到255，所以采用这种方案的缺点呢，就是我们的字符串长度。不可以超过255</u>，因为如果更大的话，
char[0]就表示不了那么多数字，对吧？但是呢，刚才有说过用方案二的好处是我们字符的位序和数组下标啊是。完全对应的，我们不需要转换，<u>所以我们王道书上的示例代码采用的一个方案，其实是方案一和方案二的一个结合。我们会舍弃，就是char[0]这个位置，我们舍弃不用，但是我们依然会专门的声明一个int型的变量。lengths用来表示，这个字符串它到底有多长？</u>那采用这种方式的话就可以兼具上面这两种方案的一个优点。
<u>所以我们之后的讲解当中也会默认的采用这样的一个方案，当我们访问一个字符串的时候，我们是从char[1]开始访问的。</u>

![image-20230920143031875](/Users/yuebinghui/Documents/program/github/note/images/image-20230920143031875.png)

## 串的链式存储

好，那这是串的顺序存储，<u>接下来看串的链式存储，其实和线性表也是一样的</u>，我们可以用每一个这样的节点来保存一个字符，一个叉。然后用一个指针指向下一个节点，<u>不过需要注意的是我们这儿一个char，它的大小只有一个字节。但是在32位的计算机当中，指针大小应该是32个比特，也就是四个字节。所以这就意味着你用一个字节的空间存储你实际想要的这个信息，还要用四个字节的空间来存储一个辅助的信息那。那这种情况我们可以把它称为存储密度低，也就是实际有用的信息所占的比例非常小</u>。

![image-20250305112146781](/Users/yuebinghui/Documents/program/github/note/images/image-20250305112146781.png)

<u>那怎么解决这个问题呢？我们可以让每一个node，每一个链表的节点，让它存储多个字符。那这个地方为了方便画图，我们只让它包含了四个字符呃，其实可以更多对吧？</u>

那这样的话，<u>在每一个节点当中使。实际有用的信息，</u>
<u>它所占的空间比例就会升高，也就是存储密度会提高，所以如果你要用列式存储来实现串的话。那一般来说比较推荐比较实用的是这种方式</u>，

还有一个细节需要注意，如果最后一个节点当中有一些位置你存不满。也就是你的字符串没有那么长的话，那你可以用某些特殊的字符把它填充进去，比如像这个地方，你用一个井号填充进去，当然你是不是也可以用？刚才我们说的杠零这个字符。好，那同样的学到这个内容的时候，大家应该下意识的回想一下，
我们在学习链表的时候聊过链表所拥有的哪些优缺点？<u>如果现在给你一个简答题，让你简述串的链式存储有什么优缺点的话，你要怎么答呢？</u>想一下是不是可以从基本操作的角度来出发，也就增删改查那些东西。<u>采用列式存储的话，你要增加或者删除呃一段空间，那是很方便的，但缺点就在于它不具备随机存取的特性。</u>

![image-20230920143538602](/Users/yuebinghui/Documents/program/github/note/images/image-20230920143538602.png)

## 基本操作的实现

和线性表的链式存储，实现其实是相通的东西好，那<u>接下来我们要探讨的问题是，我们基于这种用静态数组实现的串。怎么用代码实现与它相关的一系列基本操作？</u>
那这些基本操作的含义，我们在上一小节当中已经具体的解释过。这儿列出的这几个基本操作都是比较简单的，大家可以暂停自己来思考一下，比如像这个基本操作求串长。其实就是用这个函数返回lens值，对吧？<u>而下面这个清空操作ClearString，我们其实只需要把lens值把它设为零就可以了。只要做到这一步，我们就可以从逻辑上把这些元素清空。但实际上，在内存当中，这些元素还是存在的</u>，

![image-20230920143820913](/Users/yuebinghui/Documents/program/github/note/images/image-20230920143820913.png)

## 求子串

对吧？
这些问题我们在之前都聊过。就不再展开好下面我们要细聊的是这样的几个基本操作，第一个是求子串，我们假设有一个串s。它的长度为七呃，也就是包含了王道的七个字符好，那假设现在我们要求的是它的第四个字符，从第四个字符起。往后三个长度的子串的话，那我们需要的是不是就是gda这几个结果好，所以这个基本操作实现起来也是很简单的，<u>我们在这传入了一个啊空的串叫sub。用这个串来返回，我们想要找到的这个子串的内容</u>，然后这个<u>for循环做的事情就是把这个特定范围内的各个字符把它依次都复制到sub的这个char数组里</u>。
<u>并且还需要把sub的length值设为length</u>，像刚才这个例子，它的长度就是三，这是子串的长度，那这儿还有一个if条件判断。<u>这个if会判断我们的子串范围是否越界，比如说你要从六这个位置找到一个长度为四的子串。那么后面这两个位置本来都没有字符，那这种操作我们显然应该把它制止，也就是返回一个FALSE表示不存在这样的字串</u>

![image-20230920144330207](/Users/yuebinghui/Documents/program/github/note/images/image-20230920144330207.png)

## 比较操作

好，那这是第一个，我们要具体看的。呃，基本操作就是求子串，第二个操作就是比较两个串的大小，
其实我们在上个小节当中聊过，怎么比较两个字符串的大小？呃，也就是有这样的两个字符串，那么你要从第一个字符开始比较，如果这两个字符相同的话，那么要比较它们的下一个字符。哎，结果发现这两个字符不相同，那么就要看这两个字符到底谁更大，先出现更大字符的那个字符串。就是实际上更大的字符串，而根据这两个字符串之间的大小关系，我们的返回值可能是大于零的，可能是等于零的，
也可能是小于零的。这个代码也很简单吧，我们可以用这样的一个循环，就是从两个字符串的第一个位置开始。开始循环扫描，就是依次往后一一对比，如果发现两个字符串。对应位置的字符，他们的值不相等的话，那我们要return return这样的一个值，其实用这两个字符的值相减。就可以得到我们这想要的结果，<u>比如像刚才这个例子当中a这个字符的值要小于b这个字符的值，所以我们。返回的这个值其实就是a-b的值，</u>
<u>它是小于零的，由于a比b小，所以字符串s是要比t1这个字符串小的</u>。

那当s小于t1的时候，我们需要返回的值是小于零的值，<u>所以我们这儿直接返回a-b，也就是返回一个小于零的值是符合这儿的逻辑的</u>。

![image-20250305113304462](/Users/yuebinghui/Documents/program/github/note/images/image-20250305113304462.png)

那相应的，如果s当中这个字符的值比t当中字符的值更大，那么我们这儿的解释得到的结果就是大于零的值。同样也是符合这儿给出的这样一个规则好，<u>那如果这个for循环结束，也就是说我们已经把该对比的那些字符全部对比完了。那在这种情况下就说明，当前我们对比的这两个字符串，</u>
<u>至少前面这些部分都是完全相同的</u>。

<u>在这种情况下，我们可以return这样的一个值向右边的例子当中，我们对比s和t2这两个字符串。s的长度是7t二的长度是四，所以这个返回值返回的结果应该是七。七减四等于三，也就是大于零</u>，

那上一小节中我们说过，<u>如果这样的两个串，他们的前面这些部分都完全相同的话，那么哪个串更长？这个串就是更大的那个串，所以当s大于t2的时候，我们要返回的值是大于零的值，</u>
<u>那这个地方我们直接return一个三。所以是符合这儿给出的要求的</u>，

<u>那如果这两个字符串，它们的长度都相同，并且前面对比的所有的这些东西，这些字符也都相同的话，那么我们返回的值就应该是零。这也就意味着，两个字符串是相等的，</u>

![image-20230920144715066](/Users/yuebinghui/Documents/program/github/note/images/image-20230920144715066.png)

## 定位操作

所以同样满足这个地方给出的要求。好，那这样的话，我们就实现了两个字符串的一个比较操作。好，<u>再看下一个基本操作就是定位操作，</u>
<u>怎么在某一个主串s当中找出某一个子串t它的位置？</u>

<u>如果主串s当中没有找到和t相同的子串，那么我们的函数返回值是零就说明。t并不是s的子串</u>

好来看一下，<u>假设现在有t这样的一个字符串，它的长度length是三对吧？总共有三个字符。那我们怎么找t在s当中第一次出现的位置呢？</u>

<u>其实可以利用刚才我们实现的两个基本操作。用第一个取子串的基本操作，我们可以从s这个主串当中取下长度为三的子串，比如说先取下前面的这三个。字符作为子串，那么接下来我们可以用刚才实现的第二个基本操作，也就是对比两个字符串是否相等的基本操作来看一下诶，</u>
<u>这个子串。和t是否相等？</u>

<u>如果他们不相等，那么我们就可以取后面的一个长度为三的字串，然后再对比这两个字符串。如果还不相等呢，继续往后取对吧？</u>

![image-20250305113756311](/Users/yuebinghui/Documents/program/github/note/images/image-20250305113756311.png)

<u>好以此类推，那直到找到这样的一个子串</u>，

<u>这个子串的值和t的值对比之后，发现相等的话，那我们只需要再返回这个子串的。第一个字符，它的位序就可以了</u>，

![image-20250305113818549](/Users/yuebinghui/Documents/program/github/note/images/image-20250305113818549.png)

好，
所以捋清思路之后，这个代码就很简单了啊，这儿的第一步是求出了s的长度，也就是七。然后这个m表示的是t的长度，因为我们要知道t的长度，我们才可以知道接下来我们要取的子串到底应该取多长。好，那以这个作为参数，然后调用我们刚才实现的取子串这个基本操作，我们就可以取出长度为三的子串，然后用这样的一个while循环就可以做到。从头到尾依次取长度为三的子串，每一个子串取下来之后，我们都会调用刚才实现的这个基本操作来对比。
这个子串和t是否相等？如果这两个字符串是相等的，那么这个函数返回的结果应该是等于零的。好，所以如果不等于零的话，那么就说明还没有匹配，对吧？这样的话，我们让I加加也就是继续往后取三个字符。好，如果找到呃能够匹配的子串的话，那么就把当前这个子串的位置I给返回。这就是定位操作的实现哦，当然如果找不到相同的子串的话，那最后还要返回零表示匹配失败。
好的，这些代码其实都很简单，基础不好的同学，跨考的同学可以尝试着在纸上自己手写一遍好，那这个小节当中我们介绍了串的存储结构。分别是顺序存储和链式存储，而顺序存储又分为静态数组的实现和动态数组的实现。这些存储结构的具体实现和线性表是一模一样的，所以我们就不再多说。那需要再次强调的是，我们的王道教材当中使用的是这样的一种方案，就是会把char[0]这个位置给浪费掉。然后用一个单独的lens变量来记录这个字符串的长度，所以我们之后的示例代码也会基于这样的一种设计方案来实现。那确定了具体的存储结构之后，
同学们就可以尝试着自己实现一遍这些重要的基本操作，对于代码基础不好的同学，你自己动手做一做这样的练习其实是。可以有助于提高你的代码能力的，那这个小节当中我们是使用了一些基本操作来实现了，求一个串在主串当中的位置就实现了这个函数。从下一小节开始，我们会探讨不使用这些基本操作，而是直接来操作这个数组，然后用这种方式来实现index函数。好的，那以上就是这小节的全部内容。

## 代码

```c++
#include <stdio.h>

#define MaxLen 255
typedef char ElemType;
typedef struct {
    ElemType ch[MaxLen];
    int length;
} SString;

void StrAssign(SString &T, char chars[]) {
    int i = 0;
    while (chars[i]) {
        T.ch[i + 1] = chars[i];
        i++;
    }
    T.length = i;
}

// 求子串。用Sub返回串S的第pos个字符起，长度为Len的字串
void SubString(SString &Sub, SString S, int pos, int len) {
    if (pos + len - 1 > S.length) {
        return;
    }
    for (int i = pos; i < pos + len; i++) {
        Sub.ch[i - pos + 1] = S.ch[i];
    }
    Sub.length = len;
}

int StrCompare(SString S, SString T) {
    for (int i = 1; i <= S.length && i <= T.length; i++) {
        if (S.ch[i] != T.ch[i]) {
            return S.ch[i] - T.ch[i];
        }
    }
    return S.length - T.length;
}

int StrLength(SString S) {
    return S.length;
}

int Index(SString S, SString T) {
    int i = 1, n = StrLength(S), m = StrLength(T);
    SString sub;
    while (i <= n - m + 1) {
        SubString(sub, S, i, m);
        if (StrCompare(sub, T) != 0) {
            i++;
        } else {
            return i;
        }
    }
    return 0;
}


int main() {
    SString S;
    S.length = 0;
    SString T;
    S.length = 0;
    StrAssign(S, "wangdao");
    StrAssign(T, "n");
//    printf("%s\n", &S.ch[1]); // 打印从索引1开始的字符串
//    SString Sub;
//    SubString(Sub, S, 5, 3);
//    printf("%s\n", &Sub.ch[1]); // 打印从索引1开始的字符串

    int a = Index(S, T);

    StrCompare(S, T);


    return 0;
}
```