各位同学大家好，在这个小节中我们要学习一个重要的考点
树森林和二叉树的转换。通过之前的学习，我们知道，
当我们采用孩子兄弟表示法去存储树或者存储森林的时候。
最终会呈现出与二叉树类似的一个形态，

所以<u>树森林和二叉树之间的转换</u>
<u>本质上就是让你画出采用孩子兄弟表示法存储的树和森林</u>。
那由于这个考点比较重要，所以我们会用几个例子来学习，
怎么把<u>树转成二叉树</u>，
怎么把<u>森林转成二叉树</u>，
以及怎么把<u>二叉树转回树</u>
<u>二叉树转回森林</u>

![image-20241215115629241](/Users/yuebinghui/Documents/program/github/note/images/image-20241215115629241.png)



好，首先来看怎么把树转成二叉树，本质上就是用孩子兄弟表示法存储这棵树。
给大家总结了一个做题技巧，首先我们在二叉树当中画出这个树的根节点，接下来我们需要按树的层序依次处理各个节点。什么叫按数的程序去处理这些结点呢？也就是说我们先处理a再处理b。接下来是c再往后是dh fe JK就是一层一层，这么按顺序去处理这些节点。按这种方式去处理的话，做题的时候不容易乱好，首先我们要处理的是a这个节点处理的方法是这样的。首先，我们要观察当前处理的这个节点，在树当中有没有孩子，如果有孩子。那么就要把他所有的孩子用右指针穿成一个糖葫芦呃，
我个人觉得有点像这种冰糖葫芦哈，那对于a节点来说，它有BC。两个孩子把这两个孩子用右指针穿成一个冰糖葫芦一样的这种形态。接下来要在这个二叉树当中，把第一个孩子挂在当前处理的a结点的左指针下面，也就是这个样子。好，现在已经处理了a结点，那接下来处理b结点，对于b结点的处理方法也是一样的。观察b结点有没有孩子好？现在他有三个孩子，那首先我们要把他的三个孩子用右指针穿成一个冰糖葫芦。dh f穿起来，
接下来需要把第一个孩子连到他的左指针下面。好，那这样我们就处理完了b节点好，接下来处理c节点，他有三个孩子e JK。我们用右指针把三个孩子穿成一个冰糖葫芦，然后把第一个孩子e连到c的左指针下面。好，接下来按照树的层次顺序，我们应该处理d节点，那由于d节点没有孩子，所以对d节点不需要做任何的补充处理。好，接下来处理h他有三个孩子gil，分别用右指针把它们串起来。
然后把第一个孩子g连到h的左指针下面。好，那再往后fejk gil所有的这些节点。都没有孩子，所以后面这些节点就不需要再处理好，那这样我们就完成了数到二叉树的转换。好，再来一个例子，首先画一个根结点，从根结点出发，一层一层的去处理这些结点。那a结点有四个孩子，把四个孩子用右指针串起来，其中第一个孩子b应该连到a结点的左指针下面。好，
这样处理完了a接下来处理b。b有两个孩子d和h用右指针穿起来，同时第一个孩子d应该挂在b的左指针下面。接下来处理CC有两个孩子e和g用右指针穿起来，同时把第一个孩子连到c的左指针下面。接下来处理f结点，它只有一个孩子k，直接把它连在f的左指针下面。好再往后一个结点l他没有孩子，所以不需要处理，接下来d结点有一个孩子g。在二叉树当中g应该连在d结点的左边。好h没有孩子不需要处理e结点，只有一个孩子I在二叉树当中I应该连在e的左指针下面。好再往后，
所有的这些节点都没有孩子，所以就都不用处理。好，那这是树到二叉树的转换，接下来练习一下森林到二叉树的转换。转换的思路和之前是一样的，唯一需要注意的就是刚开始的第一步。树只有一个根结点，而森林会有多个平级的根结点。森林中所有的根结点应该视为平级的兄弟关系，所以森林转换成二叉树的时候，第一步需要把所有的这些根结点。用右指针串起来。a DJ属于兄弟关系。好，
接下来我们就可以用相同的方法按照森林的层序去处理各个节点。什么叫森林的层序呢？就是说我们要把所有的这些树看作是一个整体。第一层就是a DJ，第二层就是BC eh ij，就是一层一层的去处理。好，首先处理AA有两个孩子b和c用右指针串起来，第一个孩子b连在a的左指针下面。接下来处理de是d的孩子，所以e连在d的左边。接下来处理GG有三个孩子hi j，分别用右指针串起来，然后把第一个孩子连到g的左指针下面。接下来BC没有孩子不用处理e结点，
有一个孩子f应该把它连在e的左指针下面。h结点有两个孩子k和l，先把它们用右指针串起来，然后把第一个孩子k连在h的左边。接下来I结点没有孩子不用处理j结点，三个孩子m no先用右指针串起来，把第一个孩子连到j的左指针下面。好再往后，这些节点当中，只需要处理MM只有一个孩子p连在m的左指针下面。好，到此为止，我们就按层次的顺序处理完了每个结点。那这就是森林转换成二叉树的做题方法。好，
再来一个例子来练手。森林转换成二叉树，首先所有的这些根结点全部用右指针串起来，因为它们之间属于平级的兄弟关系。接下来，按层次顺序处理这些节点a，节点只有一个孩子b连在左边c，节点有两个孩子e和g先用右指针把它们串起来。然后第一个孩子e挂在c的左边f结点只有一个孩子k，直接连在f的左指针下面。l没有孩子不用处理，接下来b节点有d和h两个孩子用右指针串起来。再把第一个孩子d连在b的左边。再往后e结点只有一个孩子I直接连在e结点的左指针下面好，接下来再处理d。
d只有一个孩子g直接连在d的左指针下面，那这样我们就完成了森林到二叉树的转换。好，刚才是把树和森林转换成二叉树，接下来再练习把二叉树转成树和森林。同样的，我们按照树的层次顺序去恢复每个结点，那转换的技巧就是第一步先画出树的根结点。接下来，我们从这个树的根结点开始，一层一层的去恢复每个结点的孩子。同样的是，按照层次的顺序去恢复各个节点，那怎么去恢复一个节点的孩子呢？现在我们要恢复的是根节点a的孩子。
那我们要观察在这个二叉树当中，当前处理的结点a他有没有左孩子，如果有左孩子，那么就要把他的左孩子以及。一整串右指针串起来的这个冰糖葫芦啊，给拆下来也就是BC给拆下来。分别挂在当前这个结点的下方b和c挂上去。好，这样我们恢复了a结点的孩子，接下来按照层次顺序，我们恢复b结点的孩子同样的观察b的左指针诶，它的下面有孩子。接下来把右指针连起来的这一整串dh f全部拆下来，分别连到b结点的下面。这样就恢复了b结点的孩子，
接下来是c结点c结点的左指针指向了e。这是他的第一个孩子，从这个孩子开始看一下右指针串起来的这些全部拆下来。连在c的下面。好，这是对c结点的处理，那按照层次顺序，接下来应该处理d结点，由于d结点的左指针为空，说明d结点是没有孩子的。所以对于d节点不需要做恢复孩子的处理，再往后是h节点观察二叉树h的左指针指向g。从g开始把右指针连起来的这一整串gil分别拆下来，依次连到h的下面。这样就恢复了h的孩子好，
再往后的这些节点没有孩子就不需要处理，注意做题的时候，我们按照层次顺序。一层一层的去恢复每一个节点的孩子，用这样的方式去做题不容易出错，好接下来大家可以暂停把这个二叉树转。转换成数十秒钟之后公布答案。同样的，先画一个根结点，然后从根结点出发，按照程序去恢复每一个节点的孩子。那恢复的过程是这样的。好的，通过之前的学习，相信并不难理解。
好，最后我们来看二叉树怎么转换成森林原理是类似的，只不过森林当中可能会有多棵树。所以把二叉树转成森林的时候，首先我们要把二叉树的根结点和根结点右边的这一整串东西全部给拆下来。a dg分别是三棵树的根。接下来，我们按照层次顺序，依次去恢复这些结点的孩子，首先要恢复的是a结点的孩子。观察在二叉树当中a的左指针指向b，然后b的右指针这儿又连了一串东西，分别是b和c。BC一起拆下来，连到a的下面。
接下来处理d结点在二叉树当中d的左指针指向e而e的右边，没有连其他结点。所以e是d的唯一一个孩子，再来看GG的左指针连了h。h的右边这一整串hi j分别拆下来，连到g的下面，这样就恢复了g的孩子。好，到此为止，我们处理完了第一层。接下来处理第二层，对于b结点来说，它的左边没有连任何东西，所以说明它没有孩子。对于c结点来说，
它的左边没有连东西，所以c没有孩子。好，对于e来说e的左边连了f而f的右边没有串别的结点，所以f是e唯一的一个孩子。好，再看hh的左边连了k从k开始把右边这一整串kl分别拆下来，连到h的下面。好，接下来I结点这个结点的左指针没有连任何东西，所以它没有孩子好，再往后是这一结点。j的左边连了MM的，右边又穿了一串东西m no，分别拆下来连到j的下面。
好，接下来f kl他们都没有孩子。m结点它的左边连了一个p，所以p是它唯一的孩子，那到此为止，所有这些结点都已经处理完毕。同样的，按层次顺序去恢复各个节点的孩子，这么做题不容易乱好，最后再给一个例子，让大家练练手，把这棵二叉树转成森林。十秒钟之后公布答案。那转换的过程是这样的，按层次顺序依次恢复每一个节点的孩子。
好，那这是最终的答案，好的，这个小节当中我们学习了树森林和二叉树之间的相互转换。本质上就是用孩子兄弟表示法去存储树或者森林，那需要再次强调，当我们用孩子兄弟表示法去存储森林的时候。我们需要把森林当中每棵树的根结点视为平级的兄弟关系。所以我们会把森林当中每棵树的树根用右指针把它们串起来。那关于转换的方法，大家不用刻意的去记忆，只需要通过几个课后习题多加练习，你自然能够快速的转换。理解原理要比记住方法重要得多，方法只是表象，
原理才是内核。好的，那以上就是这小节的全部内容。