各位同学大家好，在这个视频中，我们会学习二零二二年新增的一个考点，
并查集好，那么当我们在探讨一个新的数据结构的时候，我们通常会。先探讨这种数据结构的啊三要素，
对吧？逻辑结构啊？物理结构，也就是存储结构以及需要实现哪些基本的操作？

![image-20241223191259585](/Users/yuebinghui/Documents/program/github/note/images/image-20241223191259585.png)

所以这个小节当中，我们首先会呃介绍并查集它的逻辑结构，
本质上它是一种集合类型的逻辑结构。属于集合这种逻辑结构在我们第一章里边给大家介绍过数据元素之间的逻辑关系，可能会呈现出集合线性关系，
树形关系和图状关系。那集合关系是我们目前暂时没有学过的，

![image-20241223191837457](/Users/yuebinghui/Documents/program/github/note/images/image-20241223191837457.png)

## 逻辑结构-集合

那集合这个概念在我们数学里边是很常用的。比如说我们一个班里的所有的同学，所有的同学
可以构成一个集合。
这个集合s abcd这些就是一个一个的同学，对吧？
每个同学就是一个数据元素，
他们从属于同一个班。s这个班

![image-20241223191906062](/Users/yuebinghui/Documents/program/github/note/images/image-20241223191906062.png)

好，那我们对这些同学是不是也可以啊？按照某些我们想要的维度进行一个分类，对吧？
把它们划分为不同的子集。

比如说不同的同学，
他喜欢吃的水果可能不一样，有的同学可能喜欢吃这种绿葡萄，

有的同学可能喜欢吃这种紫色的葡萄，有的同学可能喜欢吃橙子。

那么，我们是不是就可以把这些同学按照各自的喜好把它们划分为不同的子集绿色的这些同学，他们就喜欢吃绿葡萄的，紫色的同学就喜欢吃紫葡萄，橙色的同学就喜欢吃橙子。

好，那么各个元素划分为若干个互不相交的子集，这些子集和子集之间是互不相交的。

![image-20241223191942633](/Users/yuebinghui/Documents/program/github/note/images/image-20241223191942633.png)

好，那么在集合这种逻辑关系之下。我们任意挑选出两个元素，
这两个元素之间有什么关系呢？
比如说a和h。
它们之间的关系就是不属于同一个集合，

而a这个元素和e这个元素，它们是属于同一个集合。

所以在集合这种逻辑结构之下，两个元素之间的关系，
要么就是从属于同一个集合。
要么就是从属于不同的集合好，

![image-20241223192054060](/Users/yuebinghui/Documents/program/github/note/images/image-20241223192054060.png)

那接下来的问题是怎么用代码来表示这种集合的关系呢？
注意各个集合，各个子集之间是互不相交的，是互不相交的好，
那其实这个特性哈，我们在学树和森林的时候。
曾经接触过一个我们很熟悉的东西，

![image-20241223192721400](/Users/yuebinghui/Documents/program/github/note/images/image-20241223192721400.png)

我们来回顾一下森林的定义，森林是m棵互不相交的树的集合。
m颗互不相交的树的集合。诶，你会发现森林里边的这些树。

比如说这个图里边的这三棵树就是互不相交的，那现在我们要用代码来表示这种互不相交的集合关系。

我们是不是也可以采用树森林的这种表示方式，用这样的思路来表达出各个元素之间，它们到底是否属于同一个集合？
如果属于同一个集合的这些元素，我们在物理上可以把它组织成一棵树状的结构，
而不同集合的这些元素，我们就把它放到不同的树里。
是不是就可以了？

![image-20241223192956195](/Users/yuebinghui/Documents/program/github/note/images/image-20241223192956195.png)

好，所以这就是我们用呃代码来表示集合这种关系的一个思路。
把同一个集合里的各个元素组织成一棵树。

## 用互不相交的树，表示多个集合

好，那现在我们已经把各个元素划分为了多个不相交的集合，那我们把它们组织成多个集合之后。
我们最终的目的其实是要使用这个集合，这个数据结构对吧？
那我们对这个数据结构。会怎么使用它呢？会怎么使用它呢？
一个显而易见的需求就是如果给你指定一个元素，比如说指定元素L。让你判断这个元素，
它到底属于哪一个集合？这是常规操作，对吧？让你找到这个元素，它所属的集合，

或者如果给你指定两个元素，比如说L和h。那么，这两个元素，它们是否从属于同一个集合呢？
这是不是我们在使用这个数据结构的时候，很有可能会用到的操作啊？

![image-20241223193411104](/Users/yuebinghui/Documents/program/github/note/images/image-20241223193411104.png)

### 查

好，那这个操作怎么实现？我们把这个操作称为查的操作。
如何查到一个元素，一个指定的元素？它到底属于哪一个集合？怎么查呢？是不是很简单？

![image-20241223193431940](/Users/yuebinghui/Documents/program/github/note/images/image-20241223193431940.png)

好，那别忘了这些元素是被我们组织成了若干棵树互不相交的树，
那我们要判断一个元素，它到底从属于哪一个集合？
我们是否可以尝试着找到这个元素，就是一路往上，一路向北。找到这个元素，所属的这棵树的根结点呢？
不同的元素，它肯定从属于其中的某一棵树，对吧？
那只要找到这棵树的根结点。我们是不是就可以判断这个元素，它属于哪个集合了

![image-20241223193600560](/Users/yuebinghui/Documents/program/github/note/images/image-20241223193600560.png)

好，所以回到那个问题如何查到一个元素，它到底属于哪个集合呢？
我们可以从指定的元素出发。一路向北，一路向上，然后找到根结点，
通过这个根结点来判断它到底是属于哪一个集合那呃，

有几个根结点就是有几棵树，也就是有几个互不相交的集合，对吧？

好，那这就是查操作，查这个操作的一个

呃，具体的实现找根就可以了

![image-20241223193813059](/Users/yuebinghui/Documents/program/github/note/images/image-20241223193813059.png)

好，那刚才我们还说到另一个呃，我们可能会使用到的一个操作就是说。
我们可能会指定两个元素，让你判断判断这两个元素到底是否从属于同一个集合是吧？
是否从属于同一个集合，那很简单吧，就是分别查找分别查到这两个元素，它们的根结点，
然后对比一下这两个根结点是否是同一个？是否相同？
如果是同一个根结点，
那么这两个元素肯定就是从属于同一个集合，
如果它们的根结点不一样，那么就是从属于不同的两个集合

好，所以呃，刚才我们说到的就是判断两个元素之间。的关系，这个操作其实本质上也可以采用，刚才我们说的查的这个操作来进行实现，
只不过需要查两次而已。
好，那这是我们对集合啊，这种结构进行的第一个基本操作。好

![image-20241223193905262](/Users/yuebinghui/Documents/program/github/note/images/image-20241223193905262.png)

### 并

接下来除了查这种操作之外，我们是不是也可能会使用到并这种操作啊？
就是把两个集合合并为一个集合。
像刚才我们说绿色的这些同学，他是喜欢吃绿葡萄，紫色的这些同学，他是喜欢吃紫葡萄，然后橙色的，他是喜欢吃橙子，对吧？
那绿色和紫色的两类同学，我们是不是可以把它合并为一个集合呢？
如果有需要的话，他们都是喜欢吃葡萄的人，对吧？不管是绿葡萄还是紫葡萄，都是葡萄嘛，
所以我们有可能需要把两个互不相交的子集合并为。并为一个集合。

那合并的这个操作怎么实现？是不是很简单？
只需要让其中的一棵树。成为另一棵树的孩子，是不是就可以了？
比如说把c连到a这个根结点的下面，让这两棵树合并为同一棵树，这不就是并的操作吗？

![image-20241223194234867](/Users/yuebinghui/Documents/program/github/note/images/image-20241223194234867.png)

也就是变成这个样子。

![image-20241223194246827](/Users/yuebinghui/Documents/program/github/note/images/image-20241223194246827.png)

好，所以刚才我们说到了对这个集合要进行的，要实现的两个基本操作，
一个操作是并。把两个集合并为一个集合，
另一个操作是刚才我们说的查。就是要查找一个元素，一个指定的元素，它到底属于哪一个集合，对吧？

好讲到这儿，大家应该知道为什么这种数据结构要叫并查集了
并查集。并查集本质上是表示一种集合的逻辑关系，
而对这个集合需要实现的基本操作是并。和查所以叫并查集，懂了吧？

![image-20241223194405886](/Users/yuebinghui/Documents/program/github/note/images/image-20241223194405886.png)

好，那现在我们已经知道并查集需要实现并和查。两个基本操作以及需要表示出各个元素之间的这种集合关系，
呃，它们是从属于同一个集合还是从属于不同的集合？
好，那么接下来我们要考虑的问题就是。怎么实现代码？怎么实现代码？
那回忆一下我们在学习树那个部分的时候。树这种呃结构我们可以用哪些方法来存储呢？回忆一下，

有一种方法叫做双亲表示法，对吧？就是让孩子节点指向他的父亲节点，双亲表示法

还有一种叫孩子表示法。孩子表示法，
孩子表示法就是一个结点，它呃会呃连接后面会连接一个链表，这个链表呢，就是会包含它的各个孩子节点。
比如说a的孩子是b和c，那么呃a这个元素，它后面的链表就会指向b和c，这是孩子表示法，
如果忘记的同学可以回去看一下王道书，

![image-20241223194841065](/Users/yuebinghui/Documents/program/github/note/images/image-20241223194841065.png)

那还有一种方法叫做。孩子兄弟表示法。
孩子兄弟表示法就是呃，
同样的忘了同学回去看王道书吧，
就孩子兄弟表示法就可以把它对应为一棵二叉树嘛。



好，那现在我们要考虑的问题是我们的，并查集这种数据结构，
我们刚才说呃，它的背后本质上是对应了一棵一棵的树，对吧？
那这些树形的结构应该用双亲表示法存储，还是用孩子表示法，还是用孩子兄弟表示法来存储呢？
那显然，<u>双亲表示法</u>会更合适一些，
我们先来复习一下。双亲表示法的具体原理，然后呃，再来解释为什么双亲表示法会更适合来实现这个并查集。

来看一下这张PPT。这PPT是我们在讲树的存储的时候的一个PPT，
看一下你就可以快速的回忆了。

![image-20241223195348640](/Users/yuebinghui/Documents/program/github/note/images/image-20241223195348640.png)

a是根结点。根结点它呃所对应的这些数组元素。它没有父结点嘛，对吧？所以根结点它所对应的这个数组元素是负一
好，然后我们随便看一个g。g这个结点，它的父结点是c那c的位置在数组里面是二
那g的呃位置在数组里面是六。好，那g的父结点是c那，因此g后面对应的这个元素就是二，也就指向了c的下标，

对吧？所以呃，parent这个域，它表示的是它的父结点就是这个结点所对应的父结点它的，它在数组当中的下标位置。到底是多少？

好，那这就是双亲表示法，用一个静态的数组就可以描述出各个结点之间的这种啊，父子的关系。

方法就是让孩子结点的这个元素指针指向它的父结点所对应的数组下标。

![image-20241223195658329](/Users/yuebinghui/Documents/program/github/note/images/image-20241223195658329.png)

### 双亲表示法的好处

那这种双亲表示法有什么好处？
因为这些指针都是往上指的，所以如果给定一个结点，
你要找到这个结点，它的父结点或者一路往上找到它的根结点是不是就很容易呀？

![image-20241223195804564](/Users/yuebinghui/Documents/program/github/note/images/image-20241223195804564.png)

同时，如果你想要让两棵树并为同一棵树，并为同一棵树，
是不是只需要让另一棵树的这个呃根结点的parent指针指向其他这一棵树的根结点的编号是不是就可以了

![image-20241223195923124](/Users/yuebinghui/Documents/program/github/note/images/image-20241223195923124.png)

好，所以使用双亲表示法的好处，使用双亲表示法来表示并查集的好处就是。并和查这两个操作实现起来都非常方便，
因为查这个操作就是要一路向北，找到根结点，
而并这个操作就是要把。其中一棵树的这个呃根结点，
让它指向另一棵树的根结点。
那双亲表示，法实现起来就很方便

### 并查集的存储结构

![image-20241223200100267](/Users/yuebinghui/Documents/program/github/note/images/image-20241223200100267.png)

好，所以这就是为什么我们要用双亲表示法来存储。来表示并查集的一个原因，
那刚才我们提到这个例子abcd一直到m，
我们分别给它们对应上一个静态的数组元素。
好来看一下，比如说L
L这个元素，它的父结点是e，那看一下L对应的是这个位置。
然后它的父结点e对应的是这个位置，数组下标为四，

所以和L这个元素啊相对应的这个数组。这个数组元素的值就是四，
就是说它的父结点是四
四号结点，也就是e这个结点

![image-20241223200348293](/Users/yuebinghui/Documents/program/github/note/images/image-20241223200348293.png)

好，那e的父结点是b
对吧e的父结点是b？
那b的编号是一，
所以e这个元素，它所对应的值就是一指向它的父结点。

那b的父结点是A
A的，下标是零，
所以b这儿对应的元素就是零，
指向它的父结点，

而a这个元素它已经是根了，它已经没有父结点了，
所以a它所对应的数组。元素的值是负一
表示它是根

![image-20241223200445104](/Users/yuebinghui/Documents/program/github/note/images/image-20241223200445104.png)

好，那对于另外的两棵树也是一样的。
c和d分别是剩下两棵树的根结点，
所以它们的这这个呃数组的值分别都是负一。好
以此类推好。

因此，对于n个数据元素，我们如何表示这些数据元素之间的集合关系呢？它们是属于同一个集合还是不同的集合呢？很简单，
我们只需要声明一个长度为n。的int型数组
int型数组s，然后用这个数组就可以表示它们的集合关系。

![image-20241223200655804](/Users/yuebinghui/Documents/program/github/note/images/image-20241223200655804.png)

其实本质上就是数的双亲表示法。
所以这就是并查集的存储结构，只需要定义一个一个简单的呃长度为n的数组就可以

### 并和查的基本操作

好，接下来基于这种存储结构，我们应该如何实现刚才说到的？那两个基本操作呢，也就是并和查两个基本操作，首先来看查这种基本操作。查find想要做的事情，就是给你指定一个元素，给你指定一个元素，那么要确定指定的这个元素，它所属的集合。怎么实现？是不是很简单？只需要用这个利用这个数组，反正就是一路的往上找，
直到找到一个呃。这个这个数组的值小于零的点啊，这样的这个结点它肯定就是根结点，对吧？这样就可以确定一个元素，它所属的集合到底是哪一个好，那并这个操作是不是也很简单？比如说要把c和a，要把c和a并在一起，那我们只需要把c所对应的这个数组的值。让它指向a的下标，也就指向零，是不是就可以了？好，所以并这个操作也很简单。
因此，并查集呃英文叫disjoint set，它就是呃逻辑结构上来看，它是一种集合的逻辑结构。它是集合的一种，具体实现，只不过我们对集合的操作只需要实现并和查两种基本操作，因此它叫并查集。好，那有了思路之后，我们来实现代码，首先并查集，只需要用一个呃int型的数组来进行表示。那刚开始这些元素我们不知道，这些元素之间到底是否从属于同一个集合，
那我们可以把它们初始化为呃就是。各自独立的呃n个子集，所以初始化这个并查集，我们把所有的这个数组的元素呃数组的值全部复为呃设置为负一就可以了。表示各个元素之间，它们是各成一派的，每个元素就是独立的一个子集好，那初始化了之后，我们是不是就可以根据呃实际的业务需求啊，来对各个元素，各个集合进行一个合并。如果两个元素，应该把它们划分为同一个子集，那么把它们并到一起，是不是就OK了好？
所以接下来我们要探讨的就是并和查两个基本操作的具体代码实现。直接给出代码，首先假设这是一个并查及当前的状态。那我们要查查到某一个元素x，它到底从属于哪一个集合？是不是很简单？我们只需要啊，这个x指的是下标哈，比如说我们要找到L，我们要找到L这个元素，也就是11号元素，它所属的集合到底是哪一个？那我们while循环来处理这个数组就可以了。11号元素，它指向了四好，
所以接下来我们要检查四号元素，四号元素，它指向了一接下来我们要检查一号元素，一号元素。呃，它指向了零，所以接下来检查零号元素，零号元素，它指向了负一，那么就说明零号元素已经是L。所属的这棵树的根结点了，对吧？所以L它属于哪一个子集呢？它就属于a。a统治的这个子集。
也就是我们实现了一路往上找到根这样的一个操作简单的两句代码就可以搞定。那这儿我们return返回呃根结点的一个下标，也就是零。好，这是find操作的一个实现。好，接下来union这个操作也就是并这个操作的实现，要把两个指定的集合合并为一个。好，那首先我们要传入两个集合的根结点，比如说要把绿色和紫色这两个集合进行一个合并。那么，两个子集的根结点分别就是a和c，也就是要传入零和二，对吧？
这是a和c的。数组下标的一个编号嘛，一个编号要把这两个呃集合union合并成一个集合，应该怎么操作？好，那首先我们这儿有一个if判断，就是我们要确保当前要合并的两个集合，它是属于它本来就是两个不同的集合。就你不可能同一个集合和自己进行一个合并，对吧？这样是没有必要的，所以如果发现两个集合相同，那么直接return，不做任何处理，如果两个集合的这个根结点。
各不相同，那么进行的操作很简单，我们把。s root 2也就是s2对吧？root 2是此时是二嘛？s2的值。给它设置为root 1指向。左边这个根结点也就等于零，这个操作会导致c这个元素啊，它所对应的这个。数组的值变为零，也就是说c的父结点，我们把它指向了零号元素，也就指向了a进行了这样的一个操作，所以这就是并操作。
是不是很简单？我们要把两个集合合并为同一个集合，那么只需要把一个集合的根结点，它所对应的这个啊数组的指针让它指向另一个集合的根结点。就可以了好，那比如说我给你指定一个元素e，然后再给你指定一个元素g要合并e和g。所属的两个集合怎么办？那是不是也很简单，我们分别通过给定的这两个元素，先对它们进行find查操作，先确定它们的根结点分别是谁和谁，对吧？然后再把这两个根结点进行，一个union是不是就可以了？
好，所以如果给你指定的两个元素，它呃不是根结点。而是某一个子结点，那你要把这两个元素所从属的集合进行合并，其实先查找到根，然后再进行union进行并就可以了。OK，那这就是并查及所有相关的代码很简单，接下来我们来分析一下啊，并查及它所需要的一个时间复杂度。首先bin这个操作不用说了，它里边没有任何循环什么的，对吧？就两句简单的代码，
所以只要给出了两个集合，所属的这个根节点，那么呃进行bin操作时间复杂度只需要o1。常数集的接下来，我们重点看一下find查这个操作，它的时间复杂度是多少？好，我们来看左边这个情况。如果我们此时要查到j这个元素所属的集合，那在左边这种情况下，我们是不是只需要往上找一次？就可以找到根结点了，而如果是右边这种情况呢？我们同样是dh MI j，同样是把这几个元素组织为了一个子集，
但是在这种情况下，我们要确定j这个元素。要通过find操作确定j这个元素，它所属的集合也就是要找到根结点，是不是得一层一层一层往上找？就很麻烦对吧？所以如果这个集合，它总共有n个元素，总共有n个元素，那么。最坏的情况就是这个集合里面的这n个元素，把它组织成一棵树，这棵树瘦高瘦高的高度h刚好等于n。那在这种情况下，我们的find操作，
它的时间复杂度就需要on这个数量级。而左边这种情况就是比较好的，对吧？好，所以find操作最坏的时间复杂度是on这个是呃数量级好，现在我们应该会有一个很直观的体会。就是我们要通过find操作，找到某个元素所属的集合，这个时间复杂度最坏的时间开销。是不是直接和树的高度h相关啊？因此不难想到，如果我们想要优化这个并查集的效率的话，那我们能否在？在构造这个树的时候，让这棵树别长得太高，
让它长得尽量矮一点，对吧？这样的话。find操作的最坏时间复杂度就可以被降低嘛，就不会达到on这个量级好，接下来顺着这个思路我们来看一下怎么对呃。优脸操作进行优化。当我们要合并两个集合的时候，我们想要达到的效果就是我们希望尽可能的让数不要。长高不要长高高，让这棵树尽可能的不要长高，那一个很容易想到的方法就是。我们是否可以让一棵小树合并到一棵大树呢？什么意思？假如现在有这样的两棵树。
这棵树很大，它有很多结点，然后还有一棵树，它很小，只有只有三个结点，那我们要对这两棵树进行union。union操作的时候。我们可以怎么合？第一种方法，我们可以让左边这棵大树成为右边这棵小树的子树，对吧？但是这样是不是会导致合并了之后树的高度？增加树的高度增加。所以这种方法不太好。所以更好的方法是，
我们可以让更小的这棵树。让它成为更大的这棵树的子树，那这样的话，合并之后呃，合并之后的这棵树其实高度h是没有变化的，对吧？没有增加。所以如果每一次union，每一次合并两棵树的时候，我们都能够保证是小树合到大树的下面。那这样的话，我们就可以使得这个树的高度增加呃，没那么快好，这就是一个呃，我们的这个优化的思路。
好，那如何表示一棵树的大小呢？一个很简单的方法，我们可以用根结点的绝对值来表示树的结点总数。什么意思呢？直接看这个图就明白了a它是一个根结点a所对应的，这个数组的值。负六什么意思？a这棵树总共有六个结点，一二三四五六，所以a的值是负六。它是一个负值，但是这个负值的绝对值六，同时又表示了这棵树里下面它总共有多少个结点，对吧？
那像c，它也是一个根结点，所以c所对应的这个值这个数组的值，它也是一个负值，表示它是根，那这个负值是负二。表示c的这棵树，它总共有两个结点，总共有两个结点好那么。有了这个数组的值之后，我们是不是就可以很方便的判断到底哪棵树更小，到底哪棵树更大了？结点的总数更少的这个数就是小数，结点的总数更大的就是大数嘛，那我们要让小数合并到大数的下面是不是只需要呃修改小数的这个根结点的值？
把它指向大数对吧？大数的根结点，也就是把c所对应的这个值变成零，那除此之外，我们是不是还得把a所对应的这个数组的值？从负六变成负八对吧？变成负八，因为以前有六个结点，那你把这两棵树合并了之后，总共呃，这棵树不是总共就会有八个结点嘛？好，所以负八怎么来？是不是只需要把a和c原本的这个负六和负二进行一个相加就可以了？好，
所以我们对union操作进行优化，代码就是这样实现的。首先，我们这次要合并的是呃a和c，也就是零和二所对应的这两棵树。那第一步，我们要判断呃零和二这两棵树到底谁更小谁更大，如果s root 2也就是如果这个值。大于s root 1，因为是负值嘛，所以所以s本身的值越大，那么就说明。呃，这棵树就越是越小嘛，因此通过这个if条件就可以知道root 2要比root 1。
更小，那我们要把root 2和呃合并到root 1里边，首先刚才我们说需要把负一和负六进行一个相加，对吧相？相加之后的值要给它复回到这个呃，就是更大的这棵树，所以s root 1加等于s root 2就是要累加结点的总数。然后第二步我们要把呃二也就更小的这棵树的根结点，要让它指向更大的那棵树的根结点，所以我们把一。零的值给它赋给s root 2，也就是把这个值改成零就可以了好，那如果是else条件的话也一样的，反正就是更小的数合并到更大的数嘛。首先要累加结点的总数，
然后再把这个小数的根结点的指针指向大数的根结点。好，所以我们对用电操作进行优化之后，如果我们要合并的是这两棵树，那肯定是。紫色的这棵小树合并到呃，绿色的这棵大树这儿对吧？也就变成这个样子，同时大树的这个呃节点总数还有小树的。呃根结点的指针都需要进行修改，接下来要把这两棵树再进行合并，那显然左边这棵树。负八更大对吧？右边这棵橙色的树负五它更小，所以接下来肯定是橙色，
这棵树变成绿色，这棵树的一个子树。让d指向a那通过union这个操作之后，就会变成这个样子，那你会发现。我们两次合并操作都并没有导致整棵树的高度有任何的变化，我们都是用小数合并到大数。而如果反过来，我们不做这样的判断，是不是有可能会让大数合并到小数啊？这样的话，就会使得这个呃数的高度不断不断的增加，对吧？每一次union都会加一。呃，
这就会导致最终我们整棵树变得细长细长的，然后find就是查的时候就会效率更低嘛。好，那么我们对union操作进行优化之后。可以使得整棵树的高度不超过log 2n这样的一个数量级。这个结论可以用数学归纳法进行证明呃，有兴趣的同学可以自己去呃首推一下。好，总之以前我们对union操作没有做任何限制，最坏的情况就是每一次都让大数并到小数的下面，这就会使得每一次的并操作。都让这个树不断的长高，对吧？那个节点个节点呃，并成一棵树。
最大有可能使得这棵树的树高变成n这样的高度，对吧？那树高为n find操作的这个呃，时间复杂度是不是也会到on这个数量级？就是最坏的情况嘛，而如果我们对union操作进行限制，每一次都让小数并到大数，那就可以保证数的高度。不超过o的log n这样的一个数量级，那这样的话是不是也可以保证find操作的最坏时间复杂度？在o的log 2n这样的一个数量级上好，所以我们对union进行优化之后啊，并查集的这个整体的效率就会得到提升。而这个union操作，它的时间复杂度依然是o1，
对吧？就是执行有限的几条语句就可以了，所以union操作的复杂度。是o1，而union改了之后find的复杂度就变成了o的log 2n。好，那这就是对union操作的优化，这就是并查集的所有的代码，应该挺简单的，对吧？好的，那这个视频当中，我们学习了并查集相关的内容，我们首先介绍并查集的三要素。逻辑上来看并查集它就是一种集合元素之间，
呈现出集合的关系。两个元素要么从属于同一个集合，要么不属于同一个集合，那对于并查集这种数据结构，我们需要实现的基本操作。首先是初始化。初始化就是把所有的数组元素都初始化为负一就可以了，然后是find操作，就是查要找到指定的元素x，它所属的集合，那查的方式很简单，就是要找到x这个元素，它所属的。树的根结点，找到那个根结点就可以确定它到底属于哪个集合了，
然后接下来并union操作，就是要把两个呃不相交的集合合并为同一个集合。很简单，只需要把一个集合的根结点挂到另一个集合的根结点的下面，让它成为孩子，成为一棵子树就可以了，这是并查集的逻辑结构和基本操作，而存储结构就是采用顺序存储，其实就是一个。呃双清表示，法表示的数好，那接下来我们对union操作进行了一个优化，因为我们发现对于并查集的find操作它。它的时间复杂度和树的高度息息相关，树越矮，
那么放眼的操作，它的效率就也就越高，所以优化的方法是这样的。我们用根结点的绝对值来表示，一棵树里边总共有多少个结点，或者说一个集合里边总共有多少个结点。比如说负八表示的就是这个集合里边总共有八个元素，那每一次用union操作合并两棵树的时候都是让小树并到大树下面。这样可以保证我们n个结点。并成一棵树之后，这棵树的呃高度不超过不超过这样的一个值，所以树高不超过这样的一个值，就可以保证find操作的呃时间复杂度也不超过。欧的log rn。不超过这样的一个数量级。
好，这是对并查集的优化，大家一会儿可以呃，跟着这个导图自己回忆，然后自己手写一遍来实现一下并查集。