各位同学大家好，在这一小节中我们会学习几种特殊矩阵的压缩存储，那由于矩阵可以很方便的使用数组来存储，所以我们首先会介绍一下一维数组和二维数组背后的一些实现细节。然后我们会介绍几种特殊矩阵，分别是对称矩阵，三角矩阵，三对角矩阵和稀疏矩阵，这几种矩阵可以用一些简单的策略来节省大量的存储空间。那首先来看一下数组相关的内容，那数组大家都很熟悉，比如说C语言当中的一维数组，可以用这样的一个简单的语句就可以定义。那一个一维数组当中的各个数据元素，它们的类型都是相同的，都是alien type，
这样的类型也就意味着每一个数据元素，它们所占的大小空间都。都是相等的，并且各个数据元素在内存当中是连续存放的，所以我们只需要知道一个一维数组的起始地址，我们就可以很方便的计算出。任何一个数组下标所对应的这个元素，它的存放地址是多少？那计算的方法很简单，就是起始地址加上呃I，然后乘以每一个元素的大小。注意，我们这儿提到的数组下标，它是从零开始的，而如果数组下标不是从零开始，
而是从一开始的话，这个计算方法是不是就得变一下？这儿的I就得变成i- 1，对吧？所以大家在做题的时候一定要注意这个题目当中所给的所谓的数组下标，它应该是从零还是从一开始的，如果题目没有特别强调的话，那么。就默认是从零开始。好，那这是一维数组很简单，我们之前也聊过，接下来看二维数组，比如这个地方，我们用C语言定义了一个两行四列的一个二维数组。
同样的二维数组当中的每一个数据元素，它的数据类型其实都是相同的，也就是说每一个数据元素所需要占的存储空间也都是相同的。那从我们的逻辑视角来看，一个二维数组，其实它长的就是这个样子，就是有很多行很多列，但是内存当中的这些存储空间，它是连续的线性的。所以当我们把这个二维数组放到内存当中的时候，就需要进行一些变化，有这样的两种存储方法，第一种存储方法叫做行优先，也就是一行一行的存。就是先存第一行，
第一行存完了之后，紧接其后，再接着存第二行，这是行优先第二种存放方法是列优先。也就是先存第一列，这是第一列的两个元素，再存第二列，那这是第二列的两个元素，那之后的以此类推就是一列一列的存和一行一行的存。这就是两种不同的存储策略，分别是行优先和列优先，所以我们规定行优先或者规定列优先的存储原则。其实本质的目的是想要把这样的一个非线性的二维数组把它拉成一个线性的形状。因为刚才我们说过，计算机内存的这些存储空间都是线性的好，
那把这些数据像这样有规律的存放到内存里边之后，所带来的好处就是可以实现随机存取。也就是说，如果我们想要访问二维数组当中的某一个数据元素的话，那我们只需要给出这个元素所对应的行号和列号，那计算机就可以直接算出。这个行号和列号所对应的元素，它的存放地址到底是多少？好，首先来看，如果采用行优先存储的话，那我们怎么计算某一个元素？它的存储地址呢？其实很简单，我们只需要算出它前面的这些数据元素总共占多少个字节，
然后再加上起始地址，那这不就是这个元素，它的存放地址吗？好，所以计算方法很简单，你要计算big，它的存储地址的话，那首先是要用它的行号乘以每一行总共有多少个数据元素？然后再加上g那这个部分计算的结果就是它前面总共有多少个元素，而每一个元素所占的空间大小又应该是这么多。所以把它们一相乘，这就是big前面的这些元素所占的空间大小。然后再加上二维数组的起始地址lock，这就是big的存储地址好，所以只要我们给出一个二维数组的行号和列号。
那其实计算机就可以立即算出这个元素，所对应的存储地址，也就是说二维数组它也具有随机存取的特性。而如果采用列优先存储二维数组的话，计算的方法是不是也类似的？我们要找到某一个数据元素，它的存放地址。那么，我们同样只需要知道，它前面总共存了多少个数据元素就可以了好，所以由于列优先的存储方式是一列一列的存的。因此，我们要用列号乘以每一列总共有多少个元素，然后再加上它的含号I，那这个部分得到的结果就是在我们的目标元素之前。
总共有多少个其他的元素好，然后再乘以每个元素所占的大小，那这个部分就是它前面的这些元素所占的总共的大小。好，然后最后再加上二维数组的起始地址lock，这就是big的存储地址好，那这个地址的计算方法其实就是一个很简单的数学问题，我们在这个地方。主要是要理解什么叫列优先存储，什么叫行优先存储好，接下来我们才会正式进入这个小节的重点内容。怎么在计算机当中存储矩阵这种类型的数据？那最容易想到的是不是可以用一个二维数组来存储矩阵当中的各个元素？那这是对普通矩阵的存储，那这个地方大家会发现，
当我们在描述矩阵的时候，它的这些行号和列号都是从一开始的而。而我们在描述二维数组的时候，这些下标又通常是从零开始的，这是默认情况。当然，我们在做题的时候，还需要仔细的看一下题目给的条件到底是什么。也有可能矩阵中的这些元素，它的行号和列号也是从零开始的，这些都是比较容易错的地方。好，那普通的矩阵，我们当然可以用一个二维数组，很方便的存储，
而对于某些特殊的矩阵，我们可以用一些巧妙的方法来压缩，来节省存储空间。所以接下来我们要依次看一下这几种矩阵怎么来压缩存储。首先来看对称矩阵，不知道大家现在有没有复习线性代数学，过线性代数的话，其实对称矩阵应该是很熟悉的东西。呃，对称矩阵是什么呢？首先它是一个方阵，也就是说它是有n行n列的，而不是n行m列，就是行数和列数是相同的。它是一个方阵。
另外，对于这个矩阵中的任何一个元素都有aij=aji，在这个例子当中就是a一二=a二一。然后a一三=a三一a二三=a三二，所以这种矩阵，它的各个元素的值其实是关于这条对角线对称的。你看它们俩相同，它们俩的值相同，它们俩的值相同。当然了，如果I=j的话，是不是这个式子依然成立啊？a三三=a三三，这显然是对的，那I=j的这个区域，
也就是这条斜对角的区域。我们就把它称为主对角线，然后上面这个区域也就是行号小于列号的，我们把它称为上三角区。同理，如果行号大于列号的这个区域，我们就把它称为下三角区好，那刚才我们说过，我们可以用一个二维数组来存储这整个矩阵的数据。好，那由于对称矩阵的这两个三角区，它们的数据其实是完全相同的，因此我们在存储对称矩阵数据的时候。其实我们只需要存储其中的一个部分就可以，对吧？
好，那这就是我们压缩存储的策略，我们可以只存储这个主对角线的数据，还有下三角区的数据。当然也可以，只存储主对角线，加上三角区下面以第一种策略为例，也就只存储主对角线和下三角区。可以设置一个一维数组，然后用行优先的原则，把这些元素呃依次存进去，那所谓行优先是不是就是先存第一行？然后再存第二行，接着再存第三行，以此类推。
所以各个元素在内存中的排列就应该是这样的，这是第一行的数据，这是第二行的数据。好，那接下来要探讨两个问题，第一个问题，我们设置的这个一维数组应该设置多大呢？那第一行有一个数据，第二行两个，第三行三个。以此类推。由于这是一个n阶的对称矩阵，所以总共有n行，那第n行就应该有n个数据需要存储，因此我们总共需要存储一+2+3。
一直加到n总共需要存储这么多个数据。那这是一个简单的等差数列求和就应该是一+n×n÷2，所以我们的这个一维数组就应该是这么大。因此，最后这个数据元素，它所对应的数组下标就应该是二分之n乘以n加一再减一，对吧？这儿减一是因为我们的数组下标是从零开始的好，那这是第一个问题，我们的一位数组应该设置成这么长。接下来要探讨的是第二个问题，就站在程序员的角度来看，我们把一个矩阵的数据存储之后。最终的目的其实是想要用这些数据，对吧？
好，那当你在用这些数据的时候，你肯定是希望从你的视角看到的是一个矩阵，而不是这样的一个一维数组。也就说你肯定是希望能够从这个矩阵的视角，用矩阵的元素下标来访问这些各个元素，而不是直接使用这个一维数组的某个下标。那为了实现这个目的，其实很简单的一个方法就是你可以自己实现一个映射函数，那这样的话，当你想要访问矩阵中的某一行某一列这个元素的时候。是不是只需要用这个映射函数一转，你就可以知道诶，它存放在这个数组里边的哪个位置好，所以这是把对称矩阵压缩存储之后，
我们需要实现的很重要的一个东西。也是考研当中最喜欢考察的一个点。好，接下来就来探讨这个问题，就是怎么把矩阵的行号和列号把它映射为与之对应的数组下标。我们可以基于行优先的原则来看一下aij到底是第几个元素。好I是行号j是列号对吧？所以在它的前面应该是有i- 1行，而第一行有一个元素，第二行有两个元素，以此类推di- 1行就应该是有i- 1个元素。然后aij的j它是列号，这个列号其实也反映出了这个元素，它在本行的第几个位置？所以我们在这儿，
再加上j就可以得到aij，它到底是第几个元素？好，那最终我们要求的是它所对应的数组下标k是吧？由于数组下标k是从零开始的，所以我们在这个地方还需要减一。再次强调，有的题目它的数组下标是从一开始的，如果是那样的话，就不需要再减这个一，所以这些题目里的细节一定要注意，不要出错。好，那刚才我们只是探讨了I大于等于j的这种情况，因为I大于等于j这个区域的所有的这些元素都是按照行优先的顺序依次存到我们的一维数组当中的。
所以我们可以直接用这种方式就可以找到每一个元素，它在数组当中的实际位置，那如果要访问的是上三角区的这些元素怎么办呢？也就是I小于j的情况。这部分的元素，虽然我们没有把它实际存到这一维数组当中，但是由于对称矩阵，它拥有这样的一个美好的特性。所以其实如果行号小于列号的时候，我们可以把它转换成访问对应的aji。那这样的话，是不是又变成了行号大于列号的情况？所以就可以用和刚才相同的方法来算出aji它的实际存放位置。好，汇个总，
如果我们按行优先的方式来压缩存储对称矩阵，并且我们存储的是主对角线和下三角区，也就是橙色的这个区域的话。那么，我们要把矩阵下标转换成一维数组的下标k啊，转换的规则是这样的。这个东西不要记，应该是能够在考场上临时推出来的东西，实际的考试肯定不会考这样的原题，他肯定会改一些某一些细微的条件。比如说如果它告诉你这个对称矩阵存储主对角线和下三角区，但是按照列优先的原则来存储个元素的话。好，那这种情况怎么把矩阵下标转换成一维数组的下标呢？其实思路是一样的，
我们只需要算出aij，它到底是第几个元素就可以。那由于是按列优先的方式存储的，所以在AI jdj列之前应该是有一到j- 1列，对吧？而第一列，第一列总共有n个元素，第二列有n- 1个元素好，那把每一列的元素都给加起来。那这就是一个等差数列求和，对吧？这个部分算出的就是第j列之前总共有多少个元素？另外，不难发现，用行号I减掉j就是在同一列当中这个元素之前还有多少个元素，
比如说来看这个。用行号n- 3好，那行号减列号就是它前面总共有多少个元素，因此aij前面还应该有。呃i-j这么多个元素，那这个地方我们讨论的是aij，它到底是第几个元素？所以我们还需要再加一对吧？好了，那知道它是第几个元素之后，我们只需要再确定一下这个数组下标到底是从零开始还是从一开始那转换成数组下标就很简单了。那这是一种出题方法，除此之外，它是不是还可以存储主对角线，加上三角区，
但是做题方法都很简单好，那这是关于对称矩阵的压缩存储。接下来再看第二种特殊矩阵，三角矩阵，三角矩阵可以分为下三角矩阵和上三角矩阵，其实看图是很好理解的，所谓下三角矩阵就是指。这些部分不是常量，而上三角区这个部分都是常量，而上三角矩阵刚好相反。那由于这一整块区域所有的这些数据元素，它们的值都是相同的，所以其实我们并不需要重复的存放这么多份数据。也就是说，我们只需要重点存储，
不是常量的这个部分，那这些数据的压缩存储方法是不是和刚才对称矩阵是一样的？我们可以按照行优先或者列优先的原则，把不是常量的这个区域都放到一个一维数组里边。那这个一为数组的长度是不是一+2加一直加到n好？那除了存储这个区域之外，我们还需要再增加一个位置用来存放这个常量的值。所以整个一维数组的长度就应该是这么多，再加一也就是说相比于对称矩阵来说，存储同阶的三角矩阵，我们需要多一个这样的存储单元。好，那接下来问题是不是和对称矩阵也是一样的？我们存这个矩阵的目的，其实是为了用这个矩阵，
而用这个矩阵，我们就需要把呃横号和列号把它映射为与之对应的数组下标。那计算方法是不是和对称矩阵一毛一样啊？你看这个式子都是一样的，唯一不同的是，如果我们此次要访问的是上三角区，也就是。行号小于列号I小于j的这个区域的话。那这个区域所有的元素，它们都是一个常量c，因此访问这个区域的任何一个元素。我们都应该把它映射到这个一维数组的最后一个位置，所以上三角区的这些元素，它所对应的一维数组下标就应该是二分之n乘以n减一。好，
那这儿我们探讨的是下三角矩阵的压缩存储，那上三角矩阵是不是也是类似的？要把行号列号映射为所对应的数组下标。那我们要考虑的其实就是这个元素，它到底是第几个元素好，那按照行优先这样的一个原则，那第I行之前应该是有第一行到第i- 1行。这么多的元素，第一行总共有n个，第二行总共有n减一个，那以此类推，第二减一行就应该是。n-i+2。应该有这么多个元素好，所以第一行到第i- 1行，
总共就应该有n+n- 1一直加。加到这个值。这就是它前面那些行总共有多少个元素？那这是一个简单的等差数列求和好，那除了前面这几行之外，在第二行。呃，这个元素之前应该还有j-i这么多个元素，所以aij前面总共应该有这么多，再加上j-i。总共还有这么多个元素好，所以它对应的数组下标就应该是这个值那计算结果就应该是这样的。那这是I小于等于j的情况，如果要访问的是下三角区的话，那我们是不是需要直接把它映射给这个数组的最后一个位置，
也就映射到常量存储的这个位置？好，所以这就是三角矩阵的压缩存储。接下来要看的是三对角矩阵的压缩存储，三对角矩阵又称为带状矩阵，这种矩阵的特点就是。行号和列号，它们的绝对值相差大于一的时候，只要满足这个条件，那这些元素的值都为零。好，如果形象一些理解的话，那么呃，我们先来看中间这一条，也就是主对角线，
主对角线的这些元素。I是等于j的，对吧？也就是i-j它的绝对值应该是等于零。好，那从这个主对角线往右走一格的话I和j的差值就应该变为一对吧？那这个元素可以不为零。而如果再往右走一格的话，I和j的差值就应该是二，所以这个元素再往右肯定是一个零元素。好，那往左是不是也一样从主对角线元素往左一格这个元素，它们的差值是一，所以它可以不为零。那再往左一格那I和j的差值就应该是二，
所以这个元素肯定是零好，那上下的关系是不是也一样的？从这些主对角线元素往上一格，它们的差值变为一，所以它可以非零。再往上一个差值变为了二，所以这个元素肯定是零元素好，因此如果直观一点来理解的话，所谓的带转矩阵或者三对角矩阵就是指。所有在主对角线上的元素可以是非零元素，另外从主对角线元素出发，它们的上下左右与它们相邻的这几个元素也可以是非零元素。再往外的这些元素，就肯定是零元素好，那这是带状矩阵好，
那基于这个特点，我们其实只需要存储带状矩阵的这些非零元素就可以了。那思想都是类似的，比如我们可以按照这样行优先的原则，一行一行的存储这些非零元素。把它们依次存放到一个一维数组当中，那不难发现，对于带状矩阵来说，除了第一行和最后一行只有两个元素之外，其他每一行肯定都有三个元素。所以一个n阶的带状矩阵，我们需要存储的元素个数就应该是它总共有n行，每一行有三个元素，然后再减掉第一行，少了一个，
最后一行少了一个，也就是减掉二。它总共需要存储这么多个元素，因此这个一维数组的长度就应该是这么多，那由于我们这儿默认数组下标从零开始，因此最后一个元素，它的数组下标就应该是3 n- 3。好，接下来要解决的问题就是呃，怎么把行号列号把它映射为与之对应的数组下标？如果要访问的这个元素，它的行号和列号差值大于一的话。那么，这个区域的元素肯定就是零这些元素的值，不需要来这个数组里面找，
对吧？所以这个地方我们只探讨I和j的差值小于等于一的情况。这个范围内的元素才是存放在数组当中的，那思路和之前都是类似的，我们只需要看aij这个元素。它按照行优先的规则，到底是第几个元素就可以了，每一行都会有三个元素，然后再减掉第一行，还少了一个元素。所以前面的i- 1行应该有这么多个元素，另外不难发现aij应该是第I行的第j-i+2个元素。所以整体来看aij应该是把这两个值加起来，也就是d这么多个元素好，那如果数组下标是从零开始的，
那么转换成k的话，就还需要在这个的基础上再减一。那这样的话，我们就用行号和列号映射到了与之对应的数组下标好，那接下来如果反过来考虑，假设我们已经知道啊，数组下标k，那怎么？得到与之对应的行号和列号呢？显然，由于这个元素，它是第k+1个元素，所以k+1肯定要大于这个值，因为这个值是它。它前面的i- 1行的元素个数，
而k+1又要刚好小于等于3 i- 1，因为前I行总共应该是3 i- 1这么多个元素。这边这个不等式，把它解出来应该是这样的，那怎么让I的值刚好大于等于呃，右边这个值呢？其实只需要向上取整就可以。因为我们这儿算的I是行号嘛，所以它肯定是一个整数，因此当I取得这个值的时候，就刚好可以满足我们这儿所提到这个不等式。也就可以保证我们这儿要找的这个元素，刚好就是在第I行那王道书上给的I值的计算方法，看起来不一样。但是其实计算的结果和我们上面这儿推出的这式子是完全一样的，
大家可以自己带进去验证一下。这个式子的k值，我们可以把它理解为我们要找的这个元素，前面总共有k个元素，那k的值就应该是小于3 i- 1，然后大于等于。这个值背后的逻辑留给大家，自己去琢磨类似的这个地方的小于等于我们要把它理解为我们最终取得的I值。要刚好让这个小于等于能够得到满足，所以在解出这个部分的不等式之后，我们只需要对右边这个部分向下取整。就可以得到，刚好能够满足这个不等式的I值，那这是书上给的算法，和我们上面这个等式其实是等价的。
好，那算得I值之后，由于之前我们说过呃ki j之间有这样的一个关系，那接下来我们就可以用k和I推出j的值。所以用这样的方法，我们就可以从数组下标k得到这个元素所对应的行号和列号。注意体会，这个地方怎么来处理不等式当中刚好大于等于或者刚好小于等于这样的一个问题。好，接下来我们要看的是最后一种特殊矩阵，稀疏矩阵，所谓稀疏矩阵，就是指非零元素的个数远远少于矩阵元素的个数。你看这整个矩阵当中，非零元素只有这么几个，
寥寥无几。当然，什么叫远远少于这个，并没有一个呃固定的界限。反正作为程序员，大家应该对稀疏这个词都有一个很深刻的理解，摸摸自己的头顶就知道了。好，那对于这样的一个稀疏矩阵，如果我们定义一个和它尺寸相当的二维数组，那是不是会感觉这二维数组里边很多空间都是没有必要的，因为存的都是零。好，所以我们可以用一些技巧把它所需要的存储空间给压缩一下，第一种方法可以用顺序存储的方式来存储一系列的三元组。
每一个三元组由行号，列号还有所对应的值构成，在这里边存储的都是稀疏矩阵，里边的非零元素。比如说四这个元素，它是在第一行第三列好，那与之对应的三元组就是这个，它在第一行第三列，然后它的值是四。然后五这个元素，它在第一行第六列，所以一行六列值是五。好，那这样的三元组怎么顺序存储呢？你是不是可以定义一个struct？
destruct I里边有I有j。有value。有这样的三个字段，也就是一个struct对应这儿的一行，那你再定义一个和这个struct相对应的一维数组是不是就可以顺？顺序的存储，这些三元组了好，那这是稀疏矩阵的第一种压缩存储的方法，显然用这种方式存储稀疏矩阵的话，那你要访问其中的某一个元素。是不是只能顺序的依次扫描这些三元组，也就说会失去随机存取的特性。好，那这是第一种方法，可以用这样的一个一个的三元组来分别表示各个非零元素。
接下来看第二种方法，也是我们课本当中没有详细讲的方法，叫做十字链表法，怎么存呢？一图甚千言。你可以定义这样的一个数组，这个数组里边存放的是一个一个的指针，我们把这些指针称之为向下域。这儿的每一个指针，其实就对应了啊，稀疏矩阵当中的每一列，然后这边我们同样需要定义一个指针数组。这些指针分别对应稀疏矩阵当中的各行每一个非零元素，会对应这样的一个节点。这个节点当中包含了非零元素。
它所在的行列到底是多少？然后它的值是多少？另外还会有两个指针。来看一下这稀疏矩阵第一行的第一个非零元素是四。那么，第一个向右域这个指针，它所指向的结点对应的就是它。你看它是第一行第三列，然后值是四，然后结点的这个指针会指向同一行的下一个数据元素。那第一行的下一个非零元素应该是五，所以这个指针就指向了五这个节点，而五这个节点它是在第一行第六列。那其他各行的结点也是类似的，那向下与其实原理也是相同的，
比如说第二列，从上往下看，第一个结点应该是三。那么二，这个向下域它所指向的第一个结点就应该是三这个结点，而这个结点它所对应的元素应该是在第二行第二列，所以这儿是二二。然后这个数据结点的这个指针，它又会指向同一列的下一个非零元素。第二列的下一个非零元素应该是二，它应该在第四行第二列，所以应该是四二，然后值是二。那结合这两个图应该不难理解什么是十字链表法好的，那这个部分的内容有点多，
但是难度不大，我们主要介绍了这样的四种比较特殊的矩阵。可以用什么样的策略来压缩存储，从而节省存储空间，这就不再重复。那这个部分的考题相对来说也比较固定。好，那接下来大家再通过课后习题，对这个部分的内容进行一个实际的动手练习，再次强调呃，题目当中有可能会有这样的一些坑。那由于刚才已经说过，这儿就不再重复好，那以上就是这个小节的全部内容。
