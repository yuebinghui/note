考研当中也要求我们掌握手算求next数组的这个方法。

## 求模式串的next数组-手算练习

**所以今后我们求next数组的时候next 1我们都可以直接写0** 
**next 2我们都可以直接写一，这两个是确定的。对于任何模式串来说，一定都是这样的**

<video style="zoom:0.36" src="/Users/yuebinghui/Documents/program/github/note/笔记/数据结构/代码步骤视频/录屏2025-03-05 18.36.22.mov"></video>

## 使用next数组进行模式匹配

<video src="/Users/yuebinghui/Documents/program/github/note/笔记/数据结构/代码步骤视频/录屏2025-03-05 18.46.04.mov"></video>

## 求模式串的next数组-例讲解

<video src="/Users/yuebinghui/Documents/program/github/note/笔记/数据结构/代码步骤视频/录屏2025-03-05 18.56.47.mov"></video>

##### 题

![image-20250305190106993](/Users/yuebinghui/Documents/program/github/note/images/image-20250305190106993.png)

答案

![image-20250305190638853](/Users/yuebinghui/Documents/program/github/note/images/image-20250305190638853.png)

好，接下来我们求next 3那next 3的含义就是当第三个字符匹配失败的时候。我们应该让这指针指向什么位置好，接下来我们可以这么处理刚才画出的这条分界线。左边的这些内容是我们已知的。而右边的这些内容是我们未知的好，
所以我们可以尝试着让这个模式串一步一步的往往右移。然后我们在往右移动的过程当中注意观察这条分界线，左边的部分能不能匹配得上？好，现在先往右移动一步，那往右移一步之后发现这个g和这个o对不上，对吧？无法匹配好，所以说明我们模式串往右移到这个位置是不够的。应该继续往右移好移动到这个位置的话，整个模式串就全部移动到刚才的这个分界线的右边了而。这条分界线右边到底是呃什么样的一些字符？我们目前是暂时无法确定的，对吧？好，
因此接下来我们有必要来检查一下i所指的这个。这个元素和这所指的这个元素，它们是否能够匹配好？因此，当第三个字符。匹配失败的时候，我们可以直接让j的值等于一，也就是从这个位置。开始往后匹配。好，所以这儿我们就可以确定next 3的值next 3的值等于一。好，接下来我们要求的是next 4，也就是如果模式串的第四个字符匹配失败的话，那么应该让指针j移动到什么位置？
好来看一下，如果此时这个第四个字符匹配失败，那么和刚才一样的方法。我们可以在当前匹配失败的这个地方，给它画一条线，那这条线的左边这些字符的值，我们是可以确定的，而这条线右边我们暂时还不可以确定。好，那接下来我们让模式串一步一步的往右移。移动一步发现，这个位置无法匹配，对吧？无法匹配好，所以得继续往右移，
再移动一步发现这个位置。依然无法匹配好，所以我们还得往右移，直到整个模式串，全部都跨过了，刚才画的这条分界线。好，那就说明接下来我们应该对比的就是i所指的这个元素，以及第一个。模式串的第一个字符好，所以当这个模式串的第四个元素匹配失败的时候，我们应该让j的指针直接就指向一。直接就指向一，因此next 4应该等于一。好，
接下来到了next 5，也就是如果第五个元素匹配失败的话，应该让指针j指向哪儿？好，现在第五个元素，第五个元素匹配失败，那和刚才一样，我们在匹配失败的这个呃，这个位置前边画一条分界线，那这条分界线左边。的这些元素肯定和模式串是呃一致的。然后右边的这些元素到底是什么？我们还不得而知，好，接下来我们让这个模式串一步一步的往右移，
一边移动，我们要一边观察模式串和左边的。呃，这些主创能不能够匹配得上好？现在匹配不上对吧？显然匹配不上，然后再往右移。此时依然匹配不上好，再往右移匹配上了。绿色的这条线，左边的这些部分是能够完全匹配的好，所以接下来我们有必要检查一下i所指的位置和j所指的这个位置。这两个字符能否匹配好？因此，对于这个模式串来说，
如果第五个元素匹配失败的话，我们可以直接让j的值。等于二指向二这个位置好，所以next 5应该等于二。好，最后我们还需要确定next 6，也就是第六个元素，如果匹配失败的话，那么应该让这。指针指向什么位置好？和刚才一样，如果第六个元素匹配失败，那么呃，我们在。当前，
匹配失败的这个元素，这儿我们在这儿画一条线，然后这条线的左边这些字符，我们是可以确定的，肯定和模式串。能够对应上，但是右边的字符这条线右边的字符是什么？暂时还不能确定好，还是一样的思路，我们把模式串一步一步的。让它一步一步的往右移，然后每移动一步，你观察一下诶，这上面和下面能不能匹配上诶？现在匹配不上，
那么继续往右移，现在显然也匹配不上，所以再往右移好，现在第一个字符可以匹配上，但是第二个字符匹配不了，对吧？所以这个模式串应该再往右移好，现在这两个字符也匹配不上，因此模式串还得往右移，直到整个模式串都全部跨过了这个分界线为止。那这就说明接下来我们有必要进行检查的就是i所指的这个元素和第一个字符，能不能够匹配的上？好，所以对于google这个模式串来说，如果第六个字符匹配失败，
那么我们应该让j指针指向一这个位置，因此next 6应该等于。一好那到此为止，我们就用手算的方式确定了google这个呃字符串的next数组。确定了这个next数组，接下来我们尝试着使用这个next数组。好，我们有这样的一个主串，大家可以试一下啊，这个主串应该怎么念嗯google喽google google对吧google喽google google从这个里边要找到google这个模式串。好，现在我们已经知道了google的next数组，这是我们刚才手算的方式，求的好，那从主串位置为一的这个元素开始，
依次往后匹配。发现这些元素都能够匹配，直到。第六个元素匹配失败o和e匹配失败好，那也就是说，当j=6的时候，发生了匹配失败。好，那在这个时候我们是不是应该让j=next j，也就是等于next 6？而next 6=1，所以接下来我们应该让j的值等于一，然后i的值是保持不变的，这个我们上一节分析过对吧？好，
那j的值等于一，那就意味着接下来会从这个位置开始，继续往后匹配。好，现在i和j所指的两个元素匹配失败，匹配失败o和g对不上，对吧？那匹配失败同样的。当发生匹配失败的时候，我们就应该让j=next g，而此时j指向的是一，因此呃。进行这步操作之后j就会等于0 j=0，也就相当于指向了当前这个字符串。前边一个位置，
前边一个位置好，那上一小节，我们说过j=0，这种情况我们会进行一个特殊的处理，如果检测到j=0，那么让i和j都统一的加加。也就开始匹配下一个下一个子串好那呃，接下来这个子串第一个字符能匹配第二个能匹配第三个能匹配第四个能匹配第五个。匹配失败，第五个匹配失败，所以也就是啊j指向五的时候发生了匹配失败的情况。那此时应该让j=next j，而next j的值应该是等于二，所以接下来j的值会被修改为二。而i的值保持不变好，
因此接下来应该从这个位置继续往后匹配。好，那再往后的话，所有的这些字符对应的这些字符是不是都能够一一匹配好？那到此为止，google这个模式串就匹配成功了。那这就是对next数组的使用。好，接下来我们再用这样的一个模式串来练手，求这个模式串对应的next数组。那这个模式串它的长度是六，所以next数组也应该有next 1一直到next 6啊，这样的几个值。好，那么刚才我们说过，
对于任何一个模式，串next 1和next 2我们都可以无脑的写零和一对吧？无脑的写。好，接下来我们要求的是next 3，next 3，意味着如果第三个字符匹配失败，那么我们应该让这个模式串的指针j指向什么位置？好，现在第三个字符匹配失败，那么我们能够确定的就是前两个字符是a和b，那我们按照刚才的思路让模式串，一步一步的往后退。看一下退到什么位置可以使得这条分界线左边的部分能够完全匹配好，来看一下这个模式，
闯完又一格。那么，此时这个分界线左边的部分显然是对不上的，对不上的话，我们就只能让模式传继续往右移。好，所以我们j指针应该是在这个位置，然后i指针应该是在这个位置i指针保持不变，然后j指针是不是就应该让它指向第一个元素就是这个意思嘛？好，因此如果第三个元素匹配失败，那么应该让这指针指回到一对吧？应该让这指针指回到一。因此next 3应该是等于一的好，接下来next 4 next 4意味着当呃匹配到第四个元素的时候啊发生。
匹配失败好，那在这种情况下啊，我们同样的在匹配失败的这个这个元素，这左边这画一条分界线。然后接下来我们让模式串一步一步的往右移移动，第一步啊，现在对不上，然后移动第二步好。好，现在分界线左边的这些内容对上了能够匹配的上好，所以接下来就应该匹配主串的这个位置和模式串的这个位置。也就是应该让i保持不变，而让j指向模式串的第二个元素，对吧？这个元素的下标应该是二嘛好，
所以next 4的值就应该是二就应该是二。好，我们把它填上去，接下来算next 5 next 5意味着当第五个字符匹配失败的时候啊。指针j应该指向哪儿好？那第五个字符匹配失败的话，我们在这个字符左边这儿画一条线。好，接下来让模式串，一步一步的往右移，移一步啊，此时对不上。再移一步诶，此时对上了。就这条分界线，
左边这些部分呃，上面和下面都是可以对上的，对吧？好，所以这就说明接下来。接下来是不是让i指针保持呃原位，然后让j指针指向这个元素就可以了？也就是说，当第五个元素发生适配的时候，让这指针应该是指向这元素，也就是第三个元素，对吧？指向第三个元素，这是第一个，这是第二个，
这是第三个嘛，指向第三个元素好，所以呃next 5的值就应该是三对吧？就应该是三，我们把它填上好，最后next 6 next 6意味着如果匹配到第六个字符，发现匹配失败，那么。这个模式串应该往右移几格好，那同样的在匹配失败的这个位置，这儿我们画一条线，画一条分界线，然后让这个模式串一步一步的往右移移动一格没匹配上。再移动一个诶，对上了。
好，那这就说明接下来我们应该对比的就是这个元素和这个元素，对吧？i指针主串的i指针保持不变，然后j指针应该让它指向这个元素b，这个元素那这个元素的呃下标应该是一二三四应该是四对吧？好，因此next 6就应该等于四当第六个元素匹配失败的时候，我们可以直接让这指针指向第四个元素。然后i指针保持不变，然后从这个位置再依次往后进行匹配好的，那么到此为止，我们就求出了啊，这个模式串。它的next数组不知道大家有没有找到一点感觉啊，
我这儿介绍的这种方法是我认为就是求next数组比较快的一种手算方式。啊，这种方式和我们课本上给出的那种算法还不太一样，会更偏向于考试一些，可以帮你快速的做题。好，那最后给大家留这样的一个例子。你可以暂停自己动手算一下这个模式，串它的next数组应该是多少？可以暂停了算一下，然后我们公布答案。三二。一你算它的结果是不是这样呢？好，如果不是的话，
自己去面壁。好，对于kn p算法这个部分，你在第一次看书或者第一次看这个视频的时候，觉得很懵逼，就是听的好像似懂非懂的，这个很正常。很少有人能够在第一次学习k mp的时候，就直接把k mp完全理解，肯定都是要反复多遍的，同时你也得自己动手做一做课后习题来。来训练，因为你自己动手来模拟这个匹配的过程，以及动手求这个next数组，可以让你更清晰的理解算法背后一步一步的原理。