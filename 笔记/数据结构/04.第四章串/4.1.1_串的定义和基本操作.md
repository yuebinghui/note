学习一种新的数据结构，叫做串所谓串，其实就是字符串
那我们这一小节中会先学习串的定义和相关的基本操作。也就是要探讨它的逻辑结构，还有基本运算是什么

![image-20250228202724395](/Users/yuebinghui/Documents/program/github/note/images/image-20250228202724395.png)



## 串的定义

所谓串，其实就是字符串
那这有几个概念

串名，也就是你的这个字符串变量，它的名字，比如说这个串的串名就是s，然后下面这个串的串名就是t

还有一个概念叫串的长度，也就是说这个串里边它总共有多少个字符

如果串的长度是零，那么这种串就称之为空串

王道书上是说字符串是用单括号括起来的字符序列

JAVA c这些语言里边字符串一般，就是用一个双引号来包含的

千万不要以为这个双引号或者单引号

它是字符串里边的内容。其实它只是一个边界符，用来表示这是头这是尾而已

s这个字符串

它的长度

应该是11，而不能把这两个双引号给算进去

下面我们要认识几个，大家可能会碰到的术语

第一个术语叫子串

那所谓的子串就是指在你的这个字符串当中。任意一个连续的连续的字符组成的子序列

你把这几个字符给划起来，那其实这几个字符就是t的一个子串

![image-20250228203250182](/Users/yuebinghui/Documents/program/github/note/images/image-20250228203250182.png)

总之，只要你从这个字符串里边选择任意个连续的字符，那都可以称之为这个字符串的子串

当然了，既然是任意个，那肯定也可以是零个

一个空串也是这个字符串的子串

那与之相对应的就是主串，主串就是指包含子串的这个串，也就是指T字符串这一整坨，它就是主串

再下一个概念，叫做字符。在主串当中的位置

需要注意的是，我们的这些字符，它的编号是从一开始的，而不是从零开始的

这和我们线性表里面的位序是一样的

所以当我们在描述一个字符在主串中的位置的时候，我们要从一开始数。

比如说1这个字符，他应该是t这个字符串里边的第一二三四五六七。八第八个字符，所以他在t这个字符串中的位置就是八

那如果没有特别说明的话，我们一般探讨的是这个。这个字符它第一次出现的位置

需要注意的是，这个地方是有一个空格字符。空格，它也是一种字符

![image-20250228204550583](/Users/yuebinghui/Documents/program/github/note/images/image-20250228204550583.png)

再下一个概念叫做子串在主串中的位置

像t这个字符串里边11 pro这小段，其实就是它的一个子串，那么这个子串在主串中的位置，我们会以这个子串的第一个字符。也就是一这个字符，它的位置作为子串，在主串中的位置

那刚刚我们数过一这个字符是在第八个位置，所以11 pro这个子串，它在t里边的位置也是八

![image-20250228204543586](/Users/yuebinghui/Documents/program/github/note/images/image-20250228204543586.png)

我们这探讨的位置和线性表的位序一样，是从一开始而不是从零开始

所谓的空串和空格串是不一样的。像这个地方的m，它的单引号里边什么也没有，这是一个空串

而n这个字符串两个单引号中间其实是有很多个空格字符的，我这总共打了三个空格字符。所以n这个字符串，它的长度是三而不是零

只有长度为零的时候才称为空串

那这里边的每一个字符会占一个字节，也就是八个二进制位，八个比特

![image-20250228204846038](/Users/yuebinghui/Documents/program/github/note/images/image-20250228204846038.png)

其实串字符串这种数据结构，它的逻辑特性看起来和我们之前学习的线性表是非常相似的。线性表也是由多个数据元素组成的有序序列

串它也是一种特殊的线性表。各个数据元素之间，也就是各个字符之间，也是呈现了这种线性的关系

那它们俩的区别在于普通的线性表，它里边存放的这些数据元素可以是各种各样的数据类型。是不做限制的

而串或者说字符串这种数据结构，它的各个数据元素一般来说就是字符。比如说中文字符

英文字符，数字标点等等

所以串这种数据结构，它其实是对里边存储的这些数据元素的数据类型有了一个限制

![image-20250228205352735](/Users/yuebinghui/Documents/program/github/note/images/image-20250228205352735.png)

## 串和线性表的区别

另一个方面，<u>当我们在探讨线性表的基本操作，也就是增删改查那些操作的时候，我们通常探讨的是要增加或者删除或者查询线性表当中的某一个单独的数据元素</u>，

<u>而我们对字符串的增删改查，这些基本操作，我们通常是要以子串为操作对象</u>。

<u>那刚才说过，所谓的子串，其实就是这个串里边某几个连续的数据元素的一个有序序列</u>。

<u>就是说我们要对串这种数据结构执行增删改查的话，那通常是对这样的一堆数据元素进行增删改查</u>。其实这一点大家自己是有体会的，比如说你在使用搜索引擎搜索你想要的信息的时候，你不可能是只往搜索引擎里边输某一个字母。或者只输某一个汉字，对吧？这样肯定找不到你想要的内容，那通常我们人类的语言是要把多个字母或者多个汉字把它拼接起来。才能够表示出你想要的相应的信息。所以<u>你往搜索引擎里面输入了这样的一个字符串的时候其实就相当于你告诉这个搜索引擎说你去找一找哪些文章，里边包含了这个字串</u>，那其实我们网络里边的这些。一段一段的文字信息，
它本身就是一个很长的字符串，对吧？<u>那搜索引擎做的事情就是从这些大量的字符串里边帮你找到包含了你给出的这个子串的</u>。那些信息好，<u>那这是对串的一个查询操作是以子串为操作对象的那对字符串的增删改这些操作也是一样的，我们通常是以子串作为操作对象。而不会以某一个单独的数据元素，某一个单独的字符作为操作对象</u>好，所以这就是串和线性表的一个区别。

![image-20230920134757876](/Users/yuebinghui/Documents/program/github/note/images/image-20230920134757876.png)

## 串的基本操作

好，那既然提到了和字符串相关的基本操作，那我们来看一下我们的课本当中推荐的一些我们需要实现的基本操作。第一个操作叫做复值操作，
就是说你可以传入一个字符串chars，然后把chars这个字符串的值。付给t这个字符串。第二个是复制操作，就是把某一个串s它的值复制给t这个字符串，那这是复制操作。第三个判空操作就是判断这个字符串，它是否为空串，那其实就是判断它的长度是否为零，对吧？如果长度为零的话，返回出，否则返回for。第四个操作就是求串长，其实就是返回这个字符串的长度。
第五个操作是清空操作，就是把字符串s清空为空串。第六个是销毁串，也就是要回收你给这个字符串分配的存储空间，<u>那注意清空操作和销毁操作是有区别的。清空操作就是指你在内存当中有这样的一片小区域，存放了你的某一个字符串啊，里边的内容，比如说是ABC，那清空操作就是指你把这个字符串在逻辑上清空了。但是这一小篇内存空间你并没有还给系统，你还可以继续往里边存东西，这是清空串</u>，

<u>而所谓销毁串是指你。你把这一片的内存空间都全部归还给系统了，</u>
<u>之后这一片空间你就不能再用了，这叫销毁</u>。好，继续往后下一个操作，叫串的连接。啊，来看个例子就知道了，比如说我们这儿有一个s和w这样的两个字符串，那么我们把s和w这两个字符串连接以后，把它赋值给t。这个字符串那么结果就会导致t这个字符串它的值变成s这个字符串再拼接上w这个字符串。好，这就是字符串的拼接，那不难想到这样的一个字，符串的拼接操作有可能会导致字符串所占的存储空间的扩展增加。
因此，如果你的应用场景里边经常会需要使用到这种串的拼接连接的话，那当你在设计串的存储结构的时候，是不是得设计一种容易拓展的存储结构？那大家在了解这些基本操作的时候，也需要自己动脑想一想，就是这些基本操作背后有可能会遇到什么样的问题，就比如说刚才我们说到的这个。存储空间拓展的问题好继续。往后这个操作叫做求子串，也就是说你给了一个字符串s之后，你可以指定。你要从字符串s的第四个位置，也就是一二三四从o这个位置开始，往后六个字符。
把这六个字符作为t这个字符串的值，那执行的结果就会导致t等于。这么一串东西，<u>再次强调这儿的空格也是一个字符</u>，所以这里边其实是包含了六个字符，好再往后的一个基本操作叫做定位操作。比如说我们这儿有s和w这样的两个字符串，那么w这个字符串它其实是s的一个子串。那所谓的定位操作，其实就是尝试着寻找w这个子串，在s这个主串里边的位置。那我们之前说过，一个子串在主串当中的位置是以这个子串的第一个字母的位置作为参考的。所以其实执行这个基本操作之后的返回值就应该是11，因为p是s当中的第11个字符。
好，这就是定位操作，就是查找某一个子串在主串当中的位置，当然了，如果你传入的这个串，它并不是s的子串的话，那么我们就会返回零。

![image-20230920135229601](/Users/yuebinghui/Documents/program/github/note/images/image-20230920135229601.png)

## 串的比较操作

好，接下来看最后一个基本操作叫做<u>比较操作，其实就是比较这两个字符串谁更大？哎，那有同学可能会有疑问，这些字符又不是数字，我怎么比较它的大小呢？</u>

其实怎么比较各个字符的大小？这个大家都是很有经验的，比如说。或者说各个字符串进行过一个比值的操作了，那像这个地方abandon这个字符串的值是要小于a这个字符串的值的。好，再看下面这儿的，它的值是要大于的值，越靠前出现的这些字符串，它的值越小。越靠后出现的值越大，那大家肯定都知道它是怎么排列的，<u>abandon和aborad它前面的两个字符是一样的，但是当遇到第三个字符的时候。在英文字母表当中a是要比o先出现的。所以我们可以说a的值小于o，</u>
<u>因此这就导致了左边这个字符串的值小于右边这个字符串</u>。那下边这个例子也一样，第一个字符a是一样的，但是

<u>academic abouse对比第二个字符发现c在字母表当中要比b靠后出现。所以这就导致了academic要大于abuse。所以其实如果你要比较两个字符串的大小的话，那么就是从第一个字符开始，往后依次对比。如果哪个字符串先出现一个更大的字符，那么这个字符串它的值就更大</u>。好，这个不难理解，

![image-20230920135637344](/Users/yuebinghui/Documents/program/github/note/images/image-20230920135637344.png)

<u>那再来看一种比较特殊的情况来看，这的abstract，你把它加个后缀变成abstraction。</u>
<u>那对于这两个字符串来说，它们前边的这些字母字符都是完全一样的，但是右边的字符串它会多一个后缀，那这两个字符串也是一样的。右边这个字符串，它多了一两个空格字符，那像这种情况，我们会认为更长的那个串它的值更大</u>。所以你看这儿写的是小于号，

<u>那只有两个字符串，它的字符都相同，并且长度也都相等的时候，他们的值才是相等的</u>。好，现在再回头来理解这个比较操作，
你传入两个字符串s和t那么用，刚才我们介绍的这种规则来比较s和t的值哪个更大？如果s大于t的话，那么这个基本操作函数，它的函数返回值return的就是一个正值，如果相等的话，那么return=0，如果s。要小于t的话，那return的值就是一个负值

![image-20230920135826421](/Users/yuebinghui/Documents/program/github/note/images/image-20230920135826421.png)

## 字符集编码

好，那这就是串的比较操作，可能是大家更不容易理解的东西。好，<u>那刚才我们在谈论这两个字，符串的比值的时候，</u>
<u>我们说了这样一句话，叫做a在英文字母表当中是比o这个字母先出现的，所以a的值我们会认为它小于。o虽然刚才我们是用这样的方式理解，但其实对于计算机来说，各个字符其实在计算机当中是存储为了一个二进制数。</u>

![image-20230920140118243](/Users/yuebinghui/Documents/program/github/note/images/image-20230920140118243.png)

那这就涉及到一个所谓字符及编码的问题，因为我们的计算机，它其实只能存储二进制数，所以包括abcd这些字符在内。任何数据要存在计算机里肯定都得转换成二进制数，所以我们就得确定一个所谓的编码的规则来使得我们的各个字符和二进制数能够一一。对应比如，大多数同学可能都知道这个ask编码来看一下我们的英文字母a，它存到计算机当中之后。
其实它的高四位二进制码是这样一个数，也就是零一一零，然后它的低四位的二进制码是零零零一。所以在计算机当中存储a这个字符，它是存储了这样的一串二进制数，那类似的像c这个字符，它的高位是零一一零。然后低位是零零一一是这样的一串二进制数，所以其实计算机在对比我们的各个字符到底谁大谁小的时候。它并不是查所谓的什么英文字母表，而是直接对比它在里边存储的这个二进制数到底谁更大。那显然是c这个字符的二进制数比a这个字符的二进制数更大，因此我们会说c这个字符要大于a。当然了，科班出身的同学，
这个肯定都知道啊，但是跨考的同学还是需要注意理解一下好，那既然聊到了，<u>我们就可以从二进制数的角度来解释空格串和空串之间的区别</u>了，我们在这个地方会看到，<u>其实空格这个字符虽然在我们的计算机屏幕上。看起来是什么都没有，但是它在背后是对应了这样的一个二进制数</u>，这个二进制数是零零一零零零。这是空格，<u>这个字符在计算机当中的表示形式，所以每一个空格字符会占一个字节，也就是八个比特位</u>。好，
<u>所以这就是我们之前提到的空格串，它在背后的一个形态</u>

![image-20230920140503015](/Users/yuebinghui/Documents/program/github/note/images/image-20230920140503015.png)

![image-20230920140708124](/Users/yuebinghui/Documents/program/github/note/images/image-20230920140708124.png)

好，那到此为止，其实我们还一直没有聊什么叫做字符集。那这个集指的其实是集合，比如说我们这个地方的啊，这些英文字母，还有英文里边的标点符号，我们可以把它作为。英文里边所需要用到的字符的集合，也就是这列出的这些ask字符集。除了这些英文字母之外，呃，像我们的中文汉字还有其他一些少数民族语言，或者比如说像法语，
日语之类的。不同国家的语言都会有自己的各自的字符。比如说像我们中文里边这样的一个汉字，其实就是一个中文的字符，如果想用计算机二进制嘛来表示这些中文字符的话，那显然这样八个比特位的二进制数就不够用了。因为八个比特币的二进制数，它只能表示二的八次方，也就是256个状态。而我们的中文汉字，它有好几万个，所以256个状态是不足以区分这几万个汉字的。因此，就有一帮人提出了一个更广泛的字符的集合，比如说像uni co的字符集，
那像uni co的字符集里边就包含了我们的什么数字英文字母，当然还有我们的。我们的中文的汉字的字符好，那相应的要把这些字符集里边的字符表示为计算机当中的二进制数，那我们同样是需要确定某一种编码规则。让这些一个一个的字符映射为计算机当中的一个一个的二进制数，所以这就是所谓的给字符及编码的一个概念。比如说unicode字符集里边包含了任这个中文字符，那如果让你来设计的话，你是不是可以设计一个呃独一无二的二进制数，比如说零一零。零零什么零零一一用这串二进制数来表示，这是一个认字，对吧？好，
那这是你设计的方案，当然如果让其他人设计的话，可以让认这个字符对应另外一个二进制数。所以其实对于同一个字符集来说，你可以设计不同的编码方案，比如说大家最熟悉的是utf 8，这种编码方案当然还有utf 16等等。其实可以从数学函数的角度来理解字符集编码，你可以把字符集理解为这个函数，它的定义域。也就是里边有可能出现哪些x的值，有可能出现哪些字符，它是一个定义域，然后所谓的编码规则，比如说utf 8还有什么ask编码？
这些编码规则就相当于是你这个函数里边的映射规则f，你确定了这个映射规则之后就可以把定义域里边的x值把它映射为与之对应的y值。那么，这个y值其实就是计算机里边的这些二进制数好，那这个地方再补充一点就是，如果你采用不同的编码规则的话，那么你的每个字符它所占用的空间。大小是不同的，比如说你采用这样的编码规则，那每一个字符它只占一个字节。而如果你用utf 8这种编码规则的话，那你的一个中文字符可能是占三个字节。不过我们考研当中其实只需要考虑这种英文字母的情况，也就是说每一个字符它只占一个字节。好，
那这是给跨考同学的一个小小的拓展，



那既然聊到编码问题，我们干脆就再聊一些东西来看一下大家遇到过的这些文件乱码的问题。应该都遇到过吧，这种情况你打开某个文件的时候，发现里边的东西是一堆乱码，你都看不懂是什么意思？那很多时候产生这种乱码问题的原因其实就在于你的软件，它解码的方式出现了错误。什么意思呢？比如说在你存储你这个文件的时候，你本来是采用某一套编码规则，也就是某一个映射函数映射规则。把这些字符映射为了相对应的二进制数，比如说在你的这套规则里边，
马这个字符。把它映射为二进制数，那对应这样的一堆东西，但是当你用另一个软件来打开你刚刚串建的这个文本文件的时候，也许你的这个软件它会以为你采用的是另一套的编码。规则那在另一套的编码规则当中，可能这一串二进制数就会被映射为这样的一个奇怪的字符。所以这才导致了你在这个地方看到的是这样的一堆一堆的乱码。如果还是从刚才说的数学函数的角度来理解的话，那这个地方的码字它是自变量x，然后这个地方得到的结果它是最终的y，而你的映射规则是f，对吧？所以你用y=fx可以得到与之对应的二进制数。那你想把这个二进制数，
把它反向的解析成所对应的字符的时候，你应该使用的算法应该是x=f负一。f负1y对吧？也就使用这个函数的反函数。但是在下面这种情况里边，其实你干的事情是你搞了一个f1撇等于g负1y。你想用另一个反函数把y值反向的变成x，那这是不可能的好，所以这是从函数的角度来理解这个乱码的问题。好，那这个小节其实介绍的东西不是很难，我们花了比较多的时间讲了字符集编码，当然这个东西其实是计算机专业的学生都必须具备的一个知识。所以，科班出身的同学也不要嫌太啰嗦啊，
这也是为了照顾到其他的一些基础比较薄弱的同学好，那这个小节当中，我们介绍了串这种数据结构，它在逻辑上呈现出什么样的特性？还有需要实现哪些基本操作？那在这个章节之后的内容当中，我们会花很多的篇幅来研究和子串相关的算法。那具体来说，其实就是这个算法。也就是怎么在一个主串s当中找到它的主串t的位置，那具体怎么做，我们会用之后更多的小节来进行进一步的探索。那跨考的同学还需要特别注意怎么比较两个字符串它的大小，它在计算机底层其实就是对一堆二进制数的一个比较。那具体怎么做呢？
可以想一想大家背的四六级词汇的那个顺序版的词汇表，它是怎么排序的？好，那么以上就是这一小节的全部内容。
